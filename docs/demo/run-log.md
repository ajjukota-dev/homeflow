# Run log — 2026-09-05

Chronological record of the autonomous run. One entry per merged lane: what landed, evidence (tests, screenshots, URL), decisions taken, deviations from spec, open issues.

## 06:20 IST — run start
- Grill-me complete; decisions in TODO.md §7 items 17–23. Status board in TODO.md §0.
- Docker engine 29.1.2 started; AWS profile `pranava` verified (account 975050032697); Claude Design project created.

## 06:30 IST — R0 launched
Amarsh switched session model to Sonnet 5, said GO. Launching 4 lanes: 03 (platform/deploy), 01 (identity), 32 (design system), 02+04 (event log + canonical model). Main thread stays on Sonnet 5 for this run (Amarsh switched it himself).

## 06:45 IST — Amarsh signed off
Amarsh going to sleep; a friend will monitor GitHub. Set up `Amarsh` branch (mirrors `main`, pushed at every checkpoint) at commit 024c82c. Continuing R0 autonomously: merging lanes as they land, running R0.5 schema reconciliation, then R1+.

## 08:00 IST — R0 lanes 2, 3, 4 also finished (all four now complete)
- **Live URL:** https://we947t2rq2.ap-south-1.awsapprunner.com — `/health` returns `{"ok":true,"db":true}` against real RDS. Verified by the platform agent via live Playwright: `/` shows real East Crest data, `/home` shows Karthik Iyer real payment plan. Cost estimate from AWS Pricing API: ~$35/mo typical, up to ~$84/mo (₹2,900-6,950) worst case; $60 budget alert set.
- PR #10 (event log + canonical model): reviewed independently by main session — 167 API + 61 workspace + 9 e2e tests all re-run and green, tsc clean both packages, migrations 0002/0003 non-colliding. MERGED to main at 0491af3.
- PR #11 (design system foundation): packages/ui, tokens, fonts, ESLint token rule, axe + font + reduced-motion e2e (15 tests). Not yet reviewed/merged.
- PR #12 (platform/deploy): the live URL above, ports (db/files/mailer/pdf/clock/llm), CI, 117 API tests on PGlite+Docker Postgres+live RDS. Flags: Gmail SMTP credential is STALE (535 auth error — needs Amarsh to regenerate the app password before the live-invite demo moment works), AWS key rotation still due, a second UTC-day bug found in warranty.ts. Not yet reviewed/merged.
- PR #13 (identity/access): email/password auth, roles, seeded demo accounts (`<role>@demo.pranava` / `Demo@2026`), fixed a live cross-customer PII leak in /api/me/home. **Important gap flagged, not yet fixed**: authorize()/mask()/project-scope checks are wired only into /auth, /admin and the customer portal home route — every other existing data route still relies on client-side UI hiding only, not server enforcement. Not yet reviewed/merged.

## 08:20 IST — R0-03 (platform/deploy) merged, with a self-caught process gap
- Reconciled r0/03-platform against merged main (PR #10): resolved db.ts deletion, seed.ts/seed-lifecycle.ts canonical-column conflicts, and a real functional gap the platform port abstraction had introduced — `DbClient` had no `transaction()` method, which the event-log`\s `withTx()` needs; also `seedEventTypes()` was not wired into boot/test-db setup. Fixed both, verified 184 API + 61 workspace + 35 Playwright e2e, tsc clean, live boot smoke test.
- **Process mistake, self-caught**: those two fixes were verified against the local worktree but never `git commit`-ed before push+merge, so PR #12 landed on main WITHOUT them — main was broken for a few minutes (tsc failures, 21/35 API test files failing on `event_type_fkey`). Caught immediately by re-verifying on the actual pulled `main` (not trusting the pre-merge worktree state) — shipped as hotfix PR #14, re-verified again after. Main is green now, confirmed by a fresh `git pull` + full suite run.
- **Lesson applied going forward**: after any push, always diff the pushed remote branch/commit content directly (not just the local worktree) before calling a merge done, and always re-verify on a freshly pulled `main`, not the pre-merge worktree.

## 08:37 IST — R0-32 (design system) merged
- Jost + Geist Sans/Mono self-hosted, real sampled/scraped brand colours (packages/ui), 8 primitives + preview pages, ESLint token-literal rule (already caught and fixed real rounded-[6px] violations before this session), axe/font/reduced-motion e2e.
- Only merge conflict: root package.json (kept both the `workspaces` field and platform PR's updated description text). Re-verified: tsc clean (3 packages), 61/61 workspace unit tests, 50/50 Playwright e2e including the new design.spec.ts (24 tests: fonts load, axe 0 violations on 4 preview pages, reduced-motion collapses to <=120ms).
- Re-confirmed on freshly pulled `main`: tsc clean, 184 API + 61 workspace tests green.
- **R0 status: all four lanes (02/04 events+canonical, 03 platform/deploy, 32 design system) now merged.** Remaining R0 lane: 01 identity/access (PR #13, in progress — has a flagged security gap: authorize()/mask() only wired into a few routes).

## 09:00 IST — R0-01 (identity/access) merged. All four R0 lanes complete.
- Login, sessions (argon2id, httpOnly cookies), 33-module permission matrix, demo accounts (`<role>@demo.pranava` / `Demo@2026`, all PDF §13 roles + a customer login), invite/reset flows. Fixed a live cross-customer PII leak in /api/me/home during the build.
- Two merge rounds against 02/04, 03, 32: resolved App.tsx/Workspace.tsx nav restructuring (origin main's Projects/Units/Customers admin page spliced into identity's own ADMIN_NAV as a 4th entry), db.ts/server.ts/package-lock.json/.gitignore conflicts.
- Found and fixed 2 real bugs while re-verifying: mobile header had no way to reach any admin screen at all; one axe test raced a 440ms stagger animation and flagged mid-fade text as a contrast failure.
- Also found and fixed (unrelated to this PR): an auto-commit process swept an orphaned, already-merged worktree directory into `main` as 86 stray files, including a syntactically broken canvas file caught by an independent Codex review. Untracked all of it, gitignored `.claude/worktrees/`, `.claude/direction-last-check`, `infra/cdk.out/`. This auto-commit mechanism is outside this session's control — worth Amarsh's attention since it can silently commit things.
- Re-verified independently on freshly pulled `main` (not the pre-merge worktree): tsc clean x3 packages, 232 API + 65 workspace tests, live boot + real login smoke test against a seeded demo account.
- **R0 status: DONE. All four lanes (01, 02/04, 03, 32) merged and verified on main.** Live URL: https://we947t2rq2.ap-south-1.awsapprunner.com
- **Known gap tracked as R0.6** (not yet closed): authorize()/mask()/assertProjectScope() only wired into /api/admin/* and part of /api/me/home — every other route trusts any authenticated user regardless of role. Next up.

## 13:36 IST — R0.6 (authorization wiring) implemented, PR #15 open
- Scoped the full route inventory (49 routes: 19 in `server.ts`, 16 in `routes-lifecycle.ts`, 14 in `routes-model.ts`), confirmed against `seed/permissions.ts`'s real 32-module matrix which of the ~49 map cleanly (`authorize(ctx, module, level)`) vs. which are pre-booking master-data setup that predates the matrix (project/unit/hierarchy creation, sales-inventory browsing, booking-lifecycle admin beyond sales_handover) — Amarsh's call: role-gate the latter directly (`requireRole(ctx, allowedRoles)`, new `authz/requireRole.ts`) rather than inventing new modules.
- Threaded `ctx: Ctx` into ~50 domain handler functions across `handlers.ts`, `projects.ts`, `bookings.ts`, `bookings-crm.ts`, `demands.ts`, `demands-receipts.ts`, `customer.ts`, `collections-view.ts`, `legal-docs.ts`, `qa.ts`, `warranty.ts`, `tower-view.ts`, `events/audit.ts`, and all of `model/*.ts` — `authorize()`/`requireRole()` calls live inside the handlers (matching `auth/adminUsers.ts`'s existing pattern), not the Express route layer. Functions reused internally by an already-authorized caller (`getUnit`, `getBooking`, `listDemands`, `projectCollections`, `projectHandover`, `getCustomer`, `listApplicants`, `projectWarranty`) take `ctx` as optional and skip the check when called without it, since the outer route-facing call already authorized — avoids double-gating and keeps the "return the fresh state after a write" pattern already used throughout this codebase intact.
- Wrote `authz/route-gates.test.ts`: for a representative handler per gate type, asserts a wrong-role actor gets `{code: "forbidden"}` AND an allowed role gets past the gate — the check whose failure mode is silence otherwise proves nothing. All `authorize()`/`requireRole()` calls run before any DB row lookup, so these tests use placeholder ids safely.
- Full API suite: 248/248 passing (48 files, up from 235/47 — added `requireRole.test.ts` + `route-gates.test.ts`), `tsc --noEmit` clean. A background agent is independently verifying the frontend (workspace + my-pranava-home tsc/unit tests, root build, Playwright e2e) before merge, per the standing "test suites are a non-blocker, run in the background" instruction this session.
- **Deliberately split `mask()`/`assertProjectScope()` out as R0.6b, not silently deferred**: `mask()` only has real effect on the three modules `field_sensitivity` actually seeds (`customer_overview`, `customer_financials`, `collections`) and risks nulling fields the workspace screens haven't been checked to tolerate — shipping it blind is exactly the kind of change that passes tsc/tests green and still breaks the demo visibly. `assertProjectScope()` isn't wired anywhere yet, so a non-MANAGEMENT/SUPER_ADMIN actor scoped to one project can currently read another project's data through any of the newly-`authorize()`-gated routes — low risk today since the only seeded demo data is the single project `p_eastcrest`, but real before a second project exists. Logged as R0.6b in TODO.md §0 rather than patched into this already-large PR.

## 13:43 IST — R0.6 (authorization wiring) merged
- PR #15 merged to `main` (f9f8df6). Independent background-agent verification (separate worktree, no shared context with the authoring session): 248/248 API tests, tsc clean; 65/65 workspace unit tests, tsc clean; root `npm run build` succeeds; 67/67 Playwright e2e including `auth.spec.ts` role-landing checks for all 10+ roles and full CRM/Collections/Legal/QA/Control-tower flows; `apps/my-pranava-home` 8/8 customer-portal e2e including a real CUSTOMER-role login against the newly-gated `/api/me/home`. Zero 403/forbidden regressions anywhere.
- Re-verified independently a second time on a freshly pulled `main` (not the pre-merge worktree, not the verification agent's worktree): tsc clean, 248/248 API tests.
- CI note: GitHub Actions' `typecheck`/`test`/`docker-build` jobs show red on this PR, but confirmed (via `gh run list --branch main`) this is a **pre-existing** repo gap — `apps/workspace/package-lock.json` and `apps/my-pranava-home/package-lock.json` were never committed, so `npm ci` fails before any real test runs, on every commit to `main` today, not something this PR introduced. Logged for Amarsh in TODO.md decision #26; merged on the real (locally + independently re-run) test results rather than the broken CI signal, consistent with this session's "re-run tests yourself, don't trust the status alone" practice.
- **R0.6 done.** Remaining before R1: **R0.5** (schema reconciliation — not started) and **R0.6b** (mask()/assertProjectScope() — deliberately split out, needs a UI-tolerance check first; low risk today since demo data is single-project).

## 14:10 IST — R0.5 (schema reconciliation) done
- Read all 5 merged migrations (`0000_init.sql` through `0003_canonical.sql`) in full as ground truth, plus `gates.ts` (real `GateState`), `model/status.ts` (real DB-vs-spec vocabulary translation layer), `events/registry.ts` (real event-type registry with `built:true/false`), and `seed/permissions.ts` (real 32-module matrix) — then read all 33 spec files (`00`, `05`–`31`; `32` skipped, already built/merged) and diffed each `## Data` section, enum, module name and event name against this ground truth. Wrote `docs/specs/SCHEMA.md` as the durable reference for this going forward.
- **Real drift found and fixed in spec text** (details + exact before/after in `SCHEMA.md`): (1) spec 06's journey-instantiation trigger assumed `booking.status → CRM_ACCEPTED`, but the real `acceptBooking` (`bookings-crm.ts`) transitions `submitted → active` directly and never writes `crm_accepted` — confirmed by reading the function body, not just the earlier finding. Fixed to trigger on the real, already-emitted `sales_handover.accepted` event instead. (2) spec 07 assumed `component_definition.name`; real column is `label`. (3) spec 07 assumed a permission module `unit_progress`; the real (already-live, R0.6-gated) module is `unit_readiness`. (4) spec 08 assumed `change_category.name` (real: `customer_label`) and `change_gate_rule.trigger_state` (real: `min_state`, a `ProgressState`) — both fixed in spec text, with `trigger_event` kept as a genuinely new column (no existing analogue for procurement/drawing/handover triggers).
- **Higher-value finding than a text typo**: specs 07 and 08 each propose `CREATE TABLE`-ing `component_definition`, `change_category`, and `change_gate_rule` — all three already exist from `0000_init.sql` with a narrower column set. Their eventual migrations (`0006_progress.sql`, `0007_changeability.sql`) must `ALTER TABLE`, or the build breaks on a name collision the day someone starts typing it. Documented in `SCHEMA.md`'s collision table so this doesn't get rediscovered mid-build.
- Spec 07's proposed `unit_progress_state` (new name) vs the real `unit_progress` table (same entity, different name) is flagged as an **open decision**, not resolved unilaterally — ALTER-in-place vs a genuinely parallel table needs Amarsh's call before 07 is built.
- Fixed two stale code comments in the migrations themselves (found while checking whether earlier-session findings were still accurate): `auth_event`'s "fold into `event` once 02 lands" (02 has landed; comment now states the real reason to keep them separate) and `event.actor_user_id`'s "nullable because no user table exists yet" (01 has landed) — rechecking the second one surfaced that **no domain handler actually populates `actor_user_id`/`actor_kind` today**, even though `ctx.actor` has been available in every handler since R0.6 merged. Verified by grepping every `appendEvent()` call site across the domain layer — none pass either field. Real correctness gap for the audit trail, not just a doc fix; logged as new **R0.6c** in TODO.md rather than silently patched inside this docs-only task.
- No code behavior changed in this task beyond the two comment edits — `docs/specs/*.md` and `docs/specs/SCHEMA.md` only, plus TODO.md/run-log. **R0.5 done.**
