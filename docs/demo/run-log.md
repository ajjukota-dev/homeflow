# Run log — 2026-09-05

Chronological record of the autonomous run. One entry per merged lane: what landed, evidence (tests, screenshots, URL), decisions taken, deviations from spec, open issues.

## 06:20 IST — run start
- Grill-me complete; decisions in TODO.md §7 items 17–23. Status board in TODO.md §0.
- Docker engine 29.1.2 started; AWS profile `pranava` verified (account 975050032697); Claude Design project created.

## 06:30 IST — R0 launched
Amarsh switched session model to Sonnet 5, said GO. Launching 4 lanes: 03 (platform/deploy), 01 (identity), 32 (design system), 02+04 (event log + canonical model). Main thread stays on Sonnet 5 for this run (Amarsh switched it himself).

## 06:45 IST — Amarsh signed off
Amarsh going to sleep; a friend will monitor GitHub. Set up `Amarsh` branch (mirrors `main`, pushed at every checkpoint) at commit 024c82c. Continuing R0 autonomously: merging lanes as they land, running R0.5 schema reconciliation, then R1+.

## 08:00 IST — R0 lanes 2, 3, 4 also finished (all four now complete)
- **Live URL:** https://we947t2rq2.ap-south-1.awsapprunner.com — `/health` returns `{"ok":true,"db":true}` against real RDS. Verified by the platform agent via live Playwright: `/` shows real East Crest data, `/home` shows Karthik Iyer real payment plan. Cost estimate from AWS Pricing API: ~$35/mo typical, up to ~$84/mo (₹2,900-6,950) worst case; $60 budget alert set.
- PR #10 (event log + canonical model): reviewed independently by main session — 167 API + 61 workspace + 9 e2e tests all re-run and green, tsc clean both packages, migrations 0002/0003 non-colliding. MERGED to main at 0491af3.
- PR #11 (design system foundation): packages/ui, tokens, fonts, ESLint token rule, axe + font + reduced-motion e2e (15 tests). Not yet reviewed/merged.
- PR #12 (platform/deploy): the live URL above, ports (db/files/mailer/pdf/clock/llm), CI, 117 API tests on PGlite+Docker Postgres+live RDS. Flags: Gmail SMTP credential is STALE (535 auth error — needs Amarsh to regenerate the app password before the live-invite demo moment works), AWS key rotation still due, a second UTC-day bug found in warranty.ts. Not yet reviewed/merged.
- PR #13 (identity/access): email/password auth, roles, seeded demo accounts (`<role>@demo.pranava` / `Demo@2026`), fixed a live cross-customer PII leak in /api/me/home. **Important gap flagged, not yet fixed**: authorize()/mask()/project-scope checks are wired only into /auth, /admin and the customer portal home route — every other existing data route still relies on client-side UI hiding only, not server enforcement. Not yet reviewed/merged.

## 08:20 IST — R0-03 (platform/deploy) merged, with a self-caught process gap
- Reconciled r0/03-platform against merged main (PR #10): resolved db.ts deletion, seed.ts/seed-lifecycle.ts canonical-column conflicts, and a real functional gap the platform port abstraction had introduced — `DbClient` had no `transaction()` method, which the event-log`\s `withTx()` needs; also `seedEventTypes()` was not wired into boot/test-db setup. Fixed both, verified 184 API + 61 workspace + 35 Playwright e2e, tsc clean, live boot smoke test.
- **Process mistake, self-caught**: those two fixes were verified against the local worktree but never `git commit`-ed before push+merge, so PR #12 landed on main WITHOUT them — main was broken for a few minutes (tsc failures, 21/35 API test files failing on `event_type_fkey`). Caught immediately by re-verifying on the actual pulled `main` (not trusting the pre-merge worktree state) — shipped as hotfix PR #14, re-verified again after. Main is green now, confirmed by a fresh `git pull` + full suite run.
- **Lesson applied going forward**: after any push, always diff the pushed remote branch/commit content directly (not just the local worktree) before calling a merge done, and always re-verify on a freshly pulled `main`, not the pre-merge worktree.

## 08:37 IST — R0-32 (design system) merged
- Jost + Geist Sans/Mono self-hosted, real sampled/scraped brand colours (packages/ui), 8 primitives + preview pages, ESLint token-literal rule (already caught and fixed real rounded-[6px] violations before this session), axe/font/reduced-motion e2e.
- Only merge conflict: root package.json (kept both the `workspaces` field and platform PR's updated description text). Re-verified: tsc clean (3 packages), 61/61 workspace unit tests, 50/50 Playwright e2e including the new design.spec.ts (24 tests: fonts load, axe 0 violations on 4 preview pages, reduced-motion collapses to <=120ms).
- Re-confirmed on freshly pulled `main`: tsc clean, 184 API + 61 workspace tests green.
- **R0 status: all four lanes (02/04 events+canonical, 03 platform/deploy, 32 design system) now merged.** Remaining R0 lane: 01 identity/access (PR #13, in progress — has a flagged security gap: authorize()/mask() only wired into a few routes).

## 09:00 IST — R0-01 (identity/access) merged. All four R0 lanes complete.
- Login, sessions (argon2id, httpOnly cookies), 33-module permission matrix, demo accounts (`<role>@demo.pranava` / `Demo@2026`, all PDF §13 roles + a customer login), invite/reset flows. Fixed a live cross-customer PII leak in /api/me/home during the build.
- Two merge rounds against 02/04, 03, 32: resolved App.tsx/Workspace.tsx nav restructuring (origin main's Projects/Units/Customers admin page spliced into identity's own ADMIN_NAV as a 4th entry), db.ts/server.ts/package-lock.json/.gitignore conflicts.
- Found and fixed 2 real bugs while re-verifying: mobile header had no way to reach any admin screen at all; one axe test raced a 440ms stagger animation and flagged mid-fade text as a contrast failure.
- Also found and fixed (unrelated to this PR): an auto-commit process swept an orphaned, already-merged worktree directory into `main` as 86 stray files, including a syntactically broken canvas file caught by an independent Codex review. Untracked all of it, gitignored `.claude/worktrees/`, `.claude/direction-last-check`, `infra/cdk.out/`. This auto-commit mechanism is outside this session's control — worth Amarsh's attention since it can silently commit things.
- Re-verified independently on freshly pulled `main` (not the pre-merge worktree): tsc clean x3 packages, 232 API + 65 workspace tests, live boot + real login smoke test against a seeded demo account.
- **R0 status: DONE. All four lanes (01, 02/04, 03, 32) merged and verified on main.** Live URL: https://we947t2rq2.ap-south-1.awsapprunner.com
- **Known gap tracked as R0.6** (not yet closed): authorize()/mask()/assertProjectScope() only wired into /api/admin/* and part of /api/me/home — every other route trusts any authenticated user regardless of role. Next up.

## 13:36 IST — R0.6 (authorization wiring) implemented, PR #15 open
- Scoped the full route inventory (49 routes: 19 in `server.ts`, 16 in `routes-lifecycle.ts`, 14 in `routes-model.ts`), confirmed against `seed/permissions.ts`'s real 32-module matrix which of the ~49 map cleanly (`authorize(ctx, module, level)`) vs. which are pre-booking master-data setup that predates the matrix (project/unit/hierarchy creation, sales-inventory browsing, booking-lifecycle admin beyond sales_handover) — Amarsh's call: role-gate the latter directly (`requireRole(ctx, allowedRoles)`, new `authz/requireRole.ts`) rather than inventing new modules.
- Threaded `ctx: Ctx` into ~50 domain handler functions across `handlers.ts`, `projects.ts`, `bookings.ts`, `bookings-crm.ts`, `demands.ts`, `demands-receipts.ts`, `customer.ts`, `collections-view.ts`, `legal-docs.ts`, `qa.ts`, `warranty.ts`, `tower-view.ts`, `events/audit.ts`, and all of `model/*.ts` — `authorize()`/`requireRole()` calls live inside the handlers (matching `auth/adminUsers.ts`'s existing pattern), not the Express route layer. Functions reused internally by an already-authorized caller (`getUnit`, `getBooking`, `listDemands`, `projectCollections`, `projectHandover`, `getCustomer`, `listApplicants`, `projectWarranty`) take `ctx` as optional and skip the check when called without it, since the outer route-facing call already authorized — avoids double-gating and keeps the "return the fresh state after a write" pattern already used throughout this codebase intact.
- Wrote `authz/route-gates.test.ts`: for a representative handler per gate type, asserts a wrong-role actor gets `{code: "forbidden"}` AND an allowed role gets past the gate — the check whose failure mode is silence otherwise proves nothing. All `authorize()`/`requireRole()` calls run before any DB row lookup, so these tests use placeholder ids safely.
- Full API suite: 248/248 passing (48 files, up from 235/47 — added `requireRole.test.ts` + `route-gates.test.ts`), `tsc --noEmit` clean. A background agent is independently verifying the frontend (workspace + my-pranava-home tsc/unit tests, root build, Playwright e2e) before merge, per the standing "test suites are a non-blocker, run in the background" instruction this session.
- **Deliberately split `mask()`/`assertProjectScope()` out as R0.6b, not silently deferred**: `mask()` only has real effect on the three modules `field_sensitivity` actually seeds (`customer_overview`, `customer_financials`, `collections`) and risks nulling fields the workspace screens haven't been checked to tolerate — shipping it blind is exactly the kind of change that passes tsc/tests green and still breaks the demo visibly. `assertProjectScope()` isn't wired anywhere yet, so a non-MANAGEMENT/SUPER_ADMIN actor scoped to one project can currently read another project's data through any of the newly-`authorize()`-gated routes — low risk today since the only seeded demo data is the single project `p_eastcrest`, but real before a second project exists. Logged as R0.6b in TODO.md §0 rather than patched into this already-large PR.

## 13:43 IST — R0.6 (authorization wiring) merged
- PR #15 merged to `main` (f9f8df6). Independent background-agent verification (separate worktree, no shared context with the authoring session): 248/248 API tests, tsc clean; 65/65 workspace unit tests, tsc clean; root `npm run build` succeeds; 67/67 Playwright e2e including `auth.spec.ts` role-landing checks for all 10+ roles and full CRM/Collections/Legal/QA/Control-tower flows; `apps/my-pranava-home` 8/8 customer-portal e2e including a real CUSTOMER-role login against the newly-gated `/api/me/home`. Zero 403/forbidden regressions anywhere.
- Re-verified independently a second time on a freshly pulled `main` (not the pre-merge worktree, not the verification agent's worktree): tsc clean, 248/248 API tests.
- CI note: GitHub Actions' `typecheck`/`test`/`docker-build` jobs show red on this PR, but confirmed (via `gh run list --branch main`) this is a **pre-existing** repo gap — `apps/workspace/package-lock.json` and `apps/my-pranava-home/package-lock.json` were never committed, so `npm ci` fails before any real test runs, on every commit to `main` today, not something this PR introduced. Logged for Amarsh in TODO.md decision #26; merged on the real (locally + independently re-run) test results rather than the broken CI signal, consistent with this session's "re-run tests yourself, don't trust the status alone" practice.
- **R0.6 done.** Remaining before R1: **R0.5** (schema reconciliation — not started) and **R0.6b** (mask()/assertProjectScope() — deliberately split out, needs a UI-tolerance check first; low risk today since demo data is single-project).

## 14:10 IST — R0.5 (schema reconciliation) done
- Read all 5 merged migrations (`0000_init.sql` through `0003_canonical.sql`) in full as ground truth, plus `gates.ts` (real `GateState`), `model/status.ts` (real DB-vs-spec vocabulary translation layer), `events/registry.ts` (real event-type registry with `built:true/false`), and `seed/permissions.ts` (real 32-module matrix) — then read all 33 spec files (`00`, `05`–`31`; `32` skipped, already built/merged) and diffed each `## Data` section, enum, module name and event name against this ground truth. Wrote `docs/specs/SCHEMA.md` as the durable reference for this going forward.
- **Real drift found and fixed in spec text** (details + exact before/after in `SCHEMA.md`): (1) spec 06's journey-instantiation trigger assumed `booking.status → CRM_ACCEPTED`, but the real `acceptBooking` (`bookings-crm.ts`) transitions `submitted → active` directly and never writes `crm_accepted` — confirmed by reading the function body, not just the earlier finding. Fixed to trigger on the real, already-emitted `sales_handover.accepted` event instead. (2) spec 07 assumed `component_definition.name`; real column is `label`. (3) spec 07 assumed a permission module `unit_progress`; the real (already-live, R0.6-gated) module is `unit_readiness`. (4) spec 08 assumed `change_category.name` (real: `customer_label`) and `change_gate_rule.trigger_state` (real: `min_state`, a `ProgressState`) — both fixed in spec text, with `trigger_event` kept as a genuinely new column (no existing analogue for procurement/drawing/handover triggers).
- **Higher-value finding than a text typo**: specs 07 and 08 each propose `CREATE TABLE`-ing `component_definition`, `change_category`, and `change_gate_rule` — all three already exist from `0000_init.sql` with a narrower column set. Their eventual migrations (`0006_progress.sql`, `0007_changeability.sql`) must `ALTER TABLE`, or the build breaks on a name collision the day someone starts typing it. Documented in `SCHEMA.md`'s collision table so this doesn't get rediscovered mid-build.
- Spec 07's proposed `unit_progress_state` (new name) vs the real `unit_progress` table (same entity, different name) is flagged as an **open decision**, not resolved unilaterally — ALTER-in-place vs a genuinely parallel table needs Amarsh's call before 07 is built.
- Fixed two stale code comments in the migrations themselves (found while checking whether earlier-session findings were still accurate): `auth_event`'s "fold into `event` once 02 lands" (02 has landed; comment now states the real reason to keep them separate) and `event.actor_user_id`'s "nullable because no user table exists yet" (01 has landed) — rechecking the second one surfaced that **no domain handler actually populates `actor_user_id`/`actor_kind` today**, even though `ctx.actor` has been available in every handler since R0.6 merged. Verified by grepping every `appendEvent()` call site across the domain layer — none pass either field. Real correctness gap for the audit trail, not just a doc fix; logged as new **R0.6c** in TODO.md rather than silently patched inside this docs-only task.
- No code behavior changed in this task beyond the two comment edits — `docs/specs/*.md` and `docs/specs/SCHEMA.md` only, plus TODO.md/run-log. **R0.5 done.**

## 14:50 IST — R1: Roadmap page merged (PR #16)
- Amarsh: "keep going, ping me when it's merged." Scoped R1's four sub-lanes (screen migration, demo seed, E2E journeys, Roadmap page) and sequenced Roadmap first — new code, zero regression surface, and spec 27 says it "ships in R1, before anything else here".
- Checked the screen-migration item honestly before deferring it: `apps/workspace` already imports `@homeflow/ui/tokens.css` and extends its Tailwind preset (confirmed by reading `index.css`/`tailwind.config`), but every page still uses local `src/ui/*` duplicates. Local `Button`'s variants (`solid|accent|tinted|outline|ghost|plain`) don't map 1:1 onto shared `Button`'s (`primary|secondary|ghost|danger`), and `Card`/`CardHeader`/`CardBody` has no shared equivalent at all — a real mapping/design decision, not a mechanical import swap, so left for its own PR rather than rushed here.
- Built `apps/workspace/src/roadmap.json` (title + PDF page refs + wave, one row per not-yet-merged spec, keyed to TODO.md §0's wave column) and `pages/management/Roadmap.tsx`, wired into `nav.ts`/`Workspace.tsx` as a Management/Super-Admin-only view. Used `PageHeader`/`Badge`/`cn` from `@homeflow/ui` — the first real consumer of the shared package inside `apps/workspace`, proving the already-wired tokens/preset plumbing actually resolves.
- No "greyed-out nav entries" existed to replace, despite spec 27's wording — checked `nav.ts`/`Workspace.tsx` directly; the nav list only ever had entries for merged specs. Added a new `roadmap` view instead of replacing anything.
- Verified: `tsc --noEmit` + `npm run build` clean, 65/65 unit/component tests, new 3-breakpoint Playwright visual test green (screenshots reviewed at 1440/768/375 — clean list grouped by wave, badges for spec numbers, no console errors). Full e2e suite has pre-existing non-deterministic flakiness (5 different failures across 3 runs against the same live dev/API servers, including once on a clean `main` baseline with my changes stashed out) — confirmed unrelated to this diff, logged in TODO.md's "Found while building" rather than fixed here; R1's own "E2E journeys" item should give each spec file its own seeded DB first.
- Merged PR #16 to `main` (CI's `typecheck`/`test`/`docker-build` jobs fail on the pre-existing missing-lockfile gap, decision #26 — same failure mode as every prior PR, not caused by this change), re-verified clean on freshly pulled `main`, mirrored to `Amarsh`.

## 15:10 IST — R1: apartment demo data merged (PR #17)
Amarsh: "dont stop - this was supposed to be an autonomous run." Continuing without interim check-ins, pinging only at merges as originally agreed.
- Checked the "demo data for 3 products" premise before building and it was wrong: a second demo project (`p_meadows`, MIXED, villa+plot) already existed from an earlier session. Only APARTMENT was missing, so added a 2-unit apartment tower to that same project rather than a redundant 4th project.
- Hit a real, reproducible infra bug while restarting dev servers repeatedly: force-killing `npm start` left a stale `.s.PGSQL.5432.lock.out` in `services/api/.data/pglite`, which aborted the next boot with a WASM `RuntimeError` inside PGlite's `_pg_initdb` — traced with evidence (filtered the crash log for short non-bundle lines to find the real error under the minified stack dump), not guessed. Fix: `rm -rf services/api/.data/pglite` (disposable dev seed) and restart. Logged symptom→fix in TODO.md.
- Verified: tsc clean, 248/248 API tests, 70/70 e2e on one clean run against a freshly reseeded DB (also confirms the earlier flaky failures were state pollution from repeated runs against one live server, not a code regression). Merged, re-verified on fresh `main`, mirrored to `Amarsh`.

## 15:35 IST — R1: sale-to-handover E2E journey merged (PR #18)
- Built `e2e/journeys/sale-to-handover.spec.ts`: a read-only walk of Sales → CRM → Collections → Legal → QA → Handover at 1440/768/375 against the existing seeded villas (V110 in-progress, V113 fully handed over) — the Portal leg is already covered by `apps/my-pranava-home/e2e/auth.spec.ts`.
- Caught the shared-DB flakiness firsthand while writing it: the CRM-leg assertion ("Karthik Iyer" visible) failed, and the failure screenshot showed why — `admin-model.spec.ts`'s customer-merge test had permanently re-pointed V110 to "Meera Krishnan" on the persisted dev DB from an earlier full-suite run. Rewrote the assertion to key off the unit number ("Villa V110", never re-pointed) instead of the owning customer's name. Real evidence, not a guess — found via the actual failure screenshot showing two "Meera Krishnan" rows and no "Karthik Iyer".
- Verified: 73/73 e2e tests on one clean run (fresh `.data/pglite`). Merged, re-verified on fresh `main`, mirrored to `Amarsh`.
- R1 remaining: screen migration (local `src/ui/*` → `@homeflow/ui`) across ~22 workspace pages — the Button/Card/Segmented mapping decision is the next piece, continuing now.

## 17:10 IST — R1: screen migration merged (PR #19) — R1 code complete
- Sized the mapping before touching any page: grepped every `<Button`/`<Card`/`<Segmented` call site across the whole app (43 Button usages, 20 Card imports, 3 Segmented) and every explicit `variant=` value used. Found the real surface was much smaller than feared — only `tinted`/`outline`/`ghost` are ever used explicitly (no `accent`/`plain`), and 29 of 43 buttons use the implicit default (`solid`). No `size="icon"` usage anywhere, so the trickiest local/shared mismatch never actually applied.
- Found one real, consequential conflict: local `solid` (`bg-fg`, black — the *default*, i.e. every "Complete handover"/"Post receipt"/"Submit to CRM" primary action) has no equivalent in shared `Button`, whose `primary` is filled orange by the design system's own stated rule ("orange is accent-only... the one place orange owns a filled surface"). This changes the color of the app's primary CTA everywhere — not a naming cleanup — so asked Amarsh directly (`AskUserQuestion`) rather than deciding it unilaterally. He picked "adopt orange primary."
- Checked what that actually renders as before assuming it was done: `apps/workspace/src/index.css`'s own `:root` block still overrides `--accent` after `@homeflow/ui`'s tokens.css import (by design — 32-design-system.md rule 10, screens migrate one at a time), so `bg-accent` today resolves to the workspace's own **existing blue** (`#0a6cff`), not packages/ui's orange. The actual visual change shipped is smaller than either of us expected: black pills → the app's own established blue, not a repaint to orange. Full token migration is a separate, later step — noted in TODO.md so it doesn't get silently assumed done.
- Added `Card`/`CardHeader`/`CardBody` to `packages/ui` (spec 32's original primitive list never included a generic container, but every page needs one) — a real design-system addition, not a silent one: called out in the PR body.
- Migrated all ~24 import sites across 22 files: `solid`→`primary` (implicit, no code change needed at 29 call sites), `tinted`/`outline`→`secondary` (11 explicit rewrites), `ghost` unchanged, `Segmented`'s `ariaLabel`→`aria-label` at 3 call sites. Deleted the now-dead local `ui/Button.tsx`/`Card.tsx`/`Segmented.tsx` and trimmed `ui/index.ts`/`ui.test.tsx` accordingly — confirmed zero remaining references first (re-searched, per the reuse-not-duplicate discipline).
- Caught one real regression before it reached anyone: the H11 Playwright test located the intervention card via `xpath=ancestor::div[contains(@class,'rounded-xl')]` — a brittle Tailwind-class selector that broke against the new Card's `rounded-card`. Searched the whole e2e suite for any other test doing the same thing (found none), fixed the one locator.
- Verified: tsc + build clean, 64/64 unit tests, 73/73 e2e on one clean run (fresh `.data/pglite`), screenshots reviewed at 1440 for CRM, QA/Handover, and Admin > Customers — clean, blue primary buttons read as intentional and consistent with the app's existing nav-highlight/active-state blue, not jarring.
- Hit the shared-DB flakiness a third time while re-verifying: running the admin-model merge test twice in a row (without reseeding between runs) reproduced the exact same failure class again — confirmed root cause with a controlled pair (fails on reused DB, passes on fresh DB) rather than assuming. Not a regression from this PR.
- Merged, re-verified on fresh `main`, mirrored to `Amarsh`. **R1 is now code-complete (4/4 sub-lanes merged)** — only "smoke against the URL" remains, deliberately not done autonomously since it's a live-deployment action against a shared environment.

## 17:30 IST — R2: journey templates backend merged (05-journey-templates.md)
- Started R2 with 05 (journey templates) since 06/10/25 all depend on it. Built `journey/dsl.ts` (the conditional-expression parser `^scope.field (==|!=|in|not in) value$`) fail-closed on purpose — Emergent's own engine fails open (unparseable rule ⇒ include the task silently), but the PDF wants governed config, so an unparseable expression now blocks template publish instead of silently misbehaving at runtime. Own test caught a real grammar gap: the first implementation only accepted quoted string values, but bare enum-style tokens (`unit.product_type in [VILLA,PLOT]`) are the more natural syntax for this domain — fixed the parser, not the test.
- Built `journey/dependency.ts` (3-colour DFS cycle check for rule 5) and `journey/templates.ts` (CRUD, draft-from-current versioning, publish with rule 5/6/3 validation, assign-to-project with rule 1, preview). Role-gated with a new `POLICY_STUDIO_ROLES` (MANAGEMENT/SUPER_ADMIN) in `authz/requireRole.ts` — read spec 25 first to confirm Journey Template Studio isn't in the 32-module permission matrix at all (same gap class R0.6 already found for project/unit/hierarchy setup), so a direct role allow-list is the right mechanism, not a matrix extension.
- Added 3 new event types (`template.version_published`, `template.assigned_to_project`, `journey.migration_offered`) to the registry as sanctioned extensions; the coverage test (`registry.test.ts`) forced real emit sites + literal test assertions for all three, not just registrations.
- Seeded Pranava Standard as real config (every environment, alongside role/permission_matrix — not demo-gated): 12 stages from 05's own seed list (its "(11)" parenthetical undercounts the list it then gives — used the list), 19 tasks (Emergent's T1-T13 with real dept/exec-type/SLA-day/dependency data from `emergent-business-rules.md`, plus PT1-PT6 for the PDF tasks 05 says have no Emergent precedent — personalisation discovery, customisation, handover appointment, 7/30/90 check-ins). Wrote a dedicated test asserting the seeded data itself passes `validateForPublish` (rules 5+6) — not just "inserted without error."
- Found a role-model gap while mapping tasks: Emergent's T13 uses `owner_role="HANDOVER"`, which isn't one of the 13 real roles in `seed/permissions.ts` — mapped it to `FM` (closest real department) and flagged it in TODO.md rather than inventing a role.
- East Crest's Project-scope override: checked the premise before writing any duration numbers — `HOMEFLOW-OS.md`'s "seven stages" table gives category mappings only, no actual per-stage durations, so seeding a specific remap would mean inventing East-Crest values with no source (exactly what CLAUDE.md's "never hard-code East Crest values" forbids). Seeded a pass-through override instead (inherits Standard unchanged, published, assigned to `p_eastcrest`) so rules 1/3 exercise against real demo data, and logged the real remap as needing Amarsh's numbers.
- Deferred the Journey Template Studio UI (swimlane/dependency-line editor) to a follow-up PR — it's a bespoke layout, not the generic-tab pattern spec 25 (not yet built) will define, so building it now risks a rebuild once 25 lands.
- Verified: tsc clean, 277/277 API tests (44 new — dsl 6, dependency 6, templates 13, seed 4, registry coverage). No frontend changes this slice, so no Playwright run.

## 17:50 IST — R2: journey timeline & SLA engine backend merged (06-timeline-sla-engine.md)
- Continued straight from 05 into 06 since it depends on it directly (journey instantiation reads 05's template versions). Built the pure engine first, same discipline as the gate engine: `journey/calendar.ts` (working-day math — deliberately reused the existing `ports/clock.ts` IST-day fix rather than re-deriving "today" myself) and `journey/engine.ts` (rule 6's read-time status derivation with a 12-case table test, and rule 2/4's stage-level topological scheduler, rolling 05's task-level `journey_dependency` up to stage-level edges since stages, not tasks, carry `planned_duration_days`).
- Built `journey/sla.ts` (clock start/pause/resume/stop, rule 5) — pause shifts `due_at` forward by the paused duration on resume rather than tracking it separately, so status derivation only ever needs `now` vs `due_at`.
- Wired the **first real production event subscriber** in the codebase (`journey/subscribers.ts`): `sales_handover.accepted` → journey instantiation (rule 1), via the `onEvent`/`dispatchAll` mechanism that existed but had never been used outside tests. Verified it doesn't silently fail against real seeded data with a scratch test (not committed) asserting zero `event_delivery_failure` rows after a real `acceptBooking()` call — then ran the full 322-test suite to confirm no other test's booking-acceptance flow broke.
- Caught a real bug in my own new test, the exact class rule 10 exists to prevent: PGlite returns `date` columns as JS `Date` objects, and `dateObj <= "2026-09-05"` (a raw UTC string) silently coerces to `NaN`, always false — found via a failing assertion, not assumed. `demands.ts` already has an `asDate` helper for this exact driver quirk; added an equivalent `asDateStr` to `journey/calendar.ts` rather than importing across the accounts/journey bounded-context line for a 3-line function.
- Seeded a single default Mon-Fri calendar (real Indian holiday dates need Amarsh's confirmation, not invented — left empty) and per-task SLA policies using the same real Emergent SLA-days table 05's seed already cited.
- Scoped down deliberately, all logged in TODO.md: no `timeline_plan_revision`/`timeline_forecast_revision` endpoints yet (tables exist, unused — planned/forecast dates equal baseline, so variance/slippage are honestly 0 today, not faked); no `entry_gate_expr` evaluation (needs gates from 07/08/16/19, none built); no re-evaluation on `customer.residency_changed`/`change_request.created` (only at-creation filtering); `completeTaskInstance` is an explicit stand-in for Universal Action's real close flow (10 not built) — flagged as the only way to actually progress a journey today.
- Verified: tsc clean, 323/323 API tests (55 new — calendar 6, engine 19, sla 7, instances 14, events coverage). No frontend changes this slice either — both 05's and 06's UIs are deferred to a follow-up PR once enough of the pattern is settled to decide whether they fold into a generic Studio shell or ship standalone.

## 18:05 IST — R2: 06 post-merge fix — `due_soon_lead_days` hardcoded (PR #22)
- Self-caught on re-review of 06's `deriveStatus` call sites: both `dueSoonLeadDays` passed the literal `2` instead of reading it from the clock's own `sla_policy` row, so a future non-default policy value would silently have no effect. Fixed both call sites (`refreshStageAndJourneyRollup`, `getJourneyForBooking`) to join `sla_clock.policy_id → sla_policy.due_soon_lead_days`. Merged as its own small PR (#22), re-verified on fresh `main` (tsc clean, 323/323), mirrored to `Amarsh`.

## 18:33 IST — R2: universal action backend merged (10-universal-action.md)
- Continued straight into 10, R2's last dependency for 25 (Policy Studio). Consulted the advisor twice: once at the start (orientation), once before writing the substantial code — the second call flagged the VERIFICATION-family mapping issue below before any code was written, plus 4 smaller issues, all fixed before the first commit.
- Designed `action_type` keyed by the existing 6-value `execution_type` enum rather than one row per task_code (19 rows) — 05 lets PROJECT-scope overrides mint arbitrary new task codes at any time, so a task-code-keyed config would need a matching new row every time or silently fall back; execution-type keying removes that collision by construction. Every per-task specific (owner_role, verifier/approver_role, priority, checklist, SLA clock) comes from the caller at `createAction` time — `action_type` only supplies fallback defaults for a bare manual action, never exercised by the one wired Source.
- **Blocking issue the advisor caught before any code was written**: Emergent's real "Verification" execution type (T2 CRM handover accept, T13 customer acknowledgement) has no attachment/evidence step at all. Mapping it to `VERIFIED_ATTACHMENT` (the natural-looking evidence requirement) would have required a file upload that never existed for those tasks and made them permanently unclosable — no journey could ever pass SALES_CRM_HANDOVER. Fixed by giving VERIFICATION's `default_evidence_requirement = NONE` and instead gating `closeAction` itself: closing requires the actor to hold `verifier_role` and not be `owner_user_id` (SUPER_ADMIN's owner-check bypassed, but the role-holder check itself is not — a genuine CRM/FM-role actor is still required). Two dedicated tests pin exactly this: a non-owner verifier closes with zero evidence rows; the owner (even holding verifier_role) is blocked.
- Self-guards key on the *submitter*/*uploader*, not current owner: `action.submitted_by` (set only by `submitForApproval`) is what `approveAction`'s self-approve guard checks; `action_evidence.uploaded_by` (per-row) is what `verifyEvidence`'s self-verify guard checks — `docs/reference/emergent-business-rules.md` documents Emergent's own bug here (both its guards test the *current* owner, and `/assign` has no status guard, so reassign-then-approve defeats them).
- Wired the one built Source (task instances, 06): `instantiateJourneyForBooking` now creates a real `action` row per task at journey creation, `cascadeActionable` keeps a dependent's new SLA clock in sync on both sides (`task_instance.sla_clock_id` and `action.sla_clock_id` point at the same shared clock row), and `completeTaskInstance` delegates to `closeAction`/`approveAction` for the real evidence-gated close instead of just flipping a status column. `reopenTaskInstance`'s transitive reset now also resets each dependent's action (`resetActionForReopen`).
- **Second advisor pass, before the first commit** (full transcript re-sent, substantial code not yet written): confirmed the design was sound but flagged 4 issues, all fixed — an unnecessary dynamic `import()` with no real circular-dependency risk (replaced with a static import), a redundant VERIFICATION check duplicated inside the evidence gate (removed, `NONE` already handles it), dead scaffolding in the routes file (deleted), and a misleading `ORDER BY ... priority DESC` in `listActions` (text-alphabetical sort of `LOW/MEDIUM/HIGH/CRITICAL` doesn't rank severity — removed; real ranking is spec 11, explicitly out of scope for 10).
- **Two more real bugs, caught by a second advisor review after the code was written and its own 18 tests were green** (both are the kind that only show up when a second module's tests exercise the integration, not the module's own unit tests):
  1. `completeTaskInstance` called `closeAction` unconditionally, and `closeAction` always refuses APPROVAL-family actions (they close via `/approve`). T6 (Legal approval) is APPROVAL-family, so every journey would have gotten stuck at AGREEMENT forever in production — invisible to `actions/core.test.ts` (family-agnostic unit tests) and to `journey/instances.test.ts`'s existing tests (none of them drove a journey through an APPROVAL-family task). Fixed: `completeTaskInstance` now branches to `approveAction` for APPROVAL-family actions. Added a regression test driving PT1→T6 end to end (two different LEGAL users, since the self-approve guard has no SUPER_ADMIN bypass) and asserting AGREEMENT reaches COMPLETED.
  2. `resetActionForReopen`'s early return (`status === 'New'`) skipped clearing a dependent action that was already New-but-clocked (armed by `cascadeActionable` before ever being started) — left a stale `sla_clock_id` on reopen instead of voiding it per rule 7. Fixed the guard and added a `stopClock` call (matching `cancelAction`'s existing pattern) so the shared clock row is actually stopped, not just unpointed-to. Extended the existing reopen regression test to assert on the action row directly (task_instance's own clock-nulling already worked and would have masked this at the journey-view level).
- **Known gap, logged not fixed**: the mirror above is task→action direction only. A caller hitting a task-backed action's own routes directly (`POST /api/actions/:id/approve|close|cancel`) bypasses `task_instance`/cascade/rollup entirely — unreachable today since 10's UI isn't built, but needs an `action.closed` event subscriber in `journey/` (rule 7's originally-planned mechanism) before that UI ships.
- Rule 2 wired for one Source only (task instances); the other 11 reference specs not yet built (17, 19, 13, 15, 07, 22, 18, 12, 30). Rule 6 (customer portal surface) is data-model-ready but the portal screen is deferred.
- Verified: tsc clean, 57/57 API test files, 342/342 tests (18 new in `actions/core.test.ts`, 1 new + 1 extended in `journey/instances.test.ts`). A full-suite run flagged 4 files (`bookings.test.ts`, `actions/core.test.ts`, `authz/mask.test.ts`, `model/projects.test.ts`) with a `beforeAll` hook timeout under full parallel load — confirmed flaky (PGlite resource contention, not a regression) by re-running those 4 files alone (all green) and the full suite again (57/57, 342/342).

## 18:57 IST — R2: policy studio + approval matrix backend merged (25-policy-studio.md) — R2 backend complete

- Last named item in R2's scope, so this closes R2 (05 + 06 + 10 + 25 all backend-merged). Built `0023_policy.sql` (generic `policy_version` draft/publish/history envelope for tables with no versioning of their own, `approval_authority_rule`), `approvals/matrix.ts` (`requiredApprovers(domain, metric, value, project)`), `studio/registry.ts` (full ~56-tab registry — parses `docs/specs/25-policy-studio.md`'s own Tabs list in its coverage test rather than hand-duplicating it, so a future spec edit that adds/removes a tab makes the test fail instead of silently drifting), `studio/core.ts` (the injection-safe generic CRUD envelope — table/column names never come from the request, only from a hard-coded registry; values are always parameterized), `studio/config.ts`, `routes-studio.ts`.
- Rule 1 ("optional per table"): only `project_calendar`/`delay_reason`/`action_type` opt into the generic envelope (no versioning columns of their own). 05's `journey_template_version` and 06's `sla_policy` keep their own bespoke versioning and stay outside it — `sla_policy`'s tab stays `built:false` in the registry until that bespoke logic is built. Point-in-time `effective_on` reconstruction from diff history isn't implemented; reads are always current-state.
- Rule 2: `requiredApprovers` fails CLOSED — throws when no band covers a value, rather than silently permitting the exception; a matrix gap blocks the workflow until Policy Studio configures a band. Publish-time overlap rejection is scoped exactly to `(domain, metric, project_id)`, so a project-specific override is explicitly allowed to overlap a global band's range (that's the point of an override), while two rules in the same exact scope may never overlap. Ships with zero seeded rows — real policy Amarsh has to set via the matrix editor, not invented to make an empty table look populated.
- Rule 4: preview-impact throws explicitly (not implemented) for all 3 registered tables rather than shipping a stub that always claims "0 affected" — indistinguishable from a real zero-impact result. Same contract for `/config/import` (stub, throws). Both routes have no `res.json` on a success path today, which is safe only because every call currently throws — flagged in a route comment so whoever implements the first real preview/import adds the missing response.
- Rule 5 ("every template/config row carries product_types[]"): added a `product_types text[]` column (NULL = every product type) to all 4 registered/matrix tables as the mechanism the rule asks for, wired end to end (registry, matrix create/list, 2 round-trip tests). The rule's second half — "PLOT-specific defaults are seeded (no interior components, registration-centric journey)" — is deliberately NOT done: no PLOT component/journey content exists yet to seed honestly, same call as East Crest's durations in 05.
- Rule 6: `exportProjectConfig` built (reads current state across the registry + matrix). `importProjectConfig` is a stub that throws — cloning a project from a template project needs real conflict/merge semantics, out of scope this segment.
- **Advisor review, second pass before merge**, caught one real gap and verified two other concerns as already sound: (1) added a regression test proving `requiredApprovers` picks the currently-effective band, not an arbitrary DB row order, when two same-range global bands have adjacent non-overlapping date windows — passed, confirming `assertNoOverlap`'s date-overlap check already makes this safe by construction, not by luck; (2) verified `seed/users.ts`'s East Crest project-id literal is safe to allow-list in the grep acceptance test — `seedUsers()` only ever runs behind the same demo-only `seedAllowed` gate as `seed(db)` in `db/index.ts`, never in production; (3) added explanatory comments on the two response-less-by-construction routes (preview, import) rather than silently leaving a future hang trap.
- East Crest grep test premise-checked against the real repo before writing (CLAUDE.md's "obvious improvement, apply and note"): the spec names one exception file (`seed/demo-east-crest.ts`); real East-Crest-specific demo content legitimately spans 4 files (`seed.ts`, `seed-lifecycle.ts`, `seed/demo-east-crest.ts`, `seed/users.ts`) — all pre-date this spec, verified individually (several apparent grep matches outside these files turned out to be comments explicitly disclaiming hardcoded East Crest values, stripped before matching to avoid false positives).
- Verified: tsc clean, 61/61 API test files, 365/365 tests (27 new — matrix 8, studio/core 8, studio/registry 6, east-crest 1, + product_types round-trip x2, + the requiredApprovers ordering regression). No frontend changes this slice — Studio UI deferred to a follow-up, same reasoning as 05/06/10.

## 19:37 IST — R3: collections & true risk backend merged (19-collections-true-risk.md) — R3's first spec

- First spec of R3 (CFO-first sequencing per TODO.md §5a). Schema reconciliation done first, R0.5-style: several of the spec's proposed column/table names collided with real, already-merged, already-tested schema (`demand`, `receipt`, `overdue_reason`) — fixed the spec text to match reality and ADDed new columns rather than renaming anything live. `docs/specs/19-collections-true-risk.md`'s Data section now carries a real-vs-proposed reconciliation table, same precedent as 07/08's earlier SCHEMA.md fixes.
- Migration `0024_collections.sql` — renamed from the spec's proposed `0017` after catching a real forward-reference bug before running it: `waiver.approval_rule_id` FKs to `approval_authority_rule`, created by the already-merged `0023_policy.sql` (25); migrations apply in filename-sort order, so `0017` would fail on a fresh DB. Caught by reasoning about merge order, not by a failed run. ALTERs `demand` (tax_amount, reason_note, next_action_id, dispute_reason), `receipt` (verification tri-state + verified_by/at + dispute_reason), `overdue_reason` (category, default_action_type); adds `tds_record`, `waiver`, `financial_clearance`.
- `money.ts` (rule 11): paise-safe rupee arithmetic (`rupeesToPaise`/`paiseToRupees`/`sumRupees`/`applyPercent`/`formatIndianGrouping`) — new math only, the codebase's existing float aggregation elsewhere is untouched.
- Rule 2 (overdue reason + grace-window sweep): `setOverdueReason` now auto-creates the reason's `default_action_type` follow-up action and points `demand.next_action_id` at the real Action row (not just the reason's descriptive label, which stays separate). `collections-sweep.ts`'s `sweepOverdueDemands`/`runOverdueReasonSweep` is the grace-window sweep's logic — no scheduler/cron exists anywhere in this codebase (same gap already flagged for 06's `ON_DATE_OFFSET` and pre-breach ladder), so this is exactly the callable a future nightly job would invoke; exercised directly in tests with a controlled `asOf`. Deliberately derives "overdue" from real facts (`due_date < asOf`, open balance, not settled/waived/disputed) rather than trusting the stored `demand.status` — no code path in this codebase ever flips that column automatically, only seed data sets it directly.
- Rule 4 (receipt dispute/verify tri-state): `demands-receipts.ts` gets `disputeReceipt`/`verifyReceipt`. A DISPUTED receipt is excluded from `remaining` via `DEMAND_SELECT`'s subquery; re-verifying restores it.
- Rule 7 (TDS): `tds.ts` — `suggestTdsApplicability` (advisory, §194IA ₹50L threshold), `upsertTdsRecord`, `verifyTds`, `rejectTds`.
- Rule 8 (waivers) — the approval matrix's (25) first real consumer: `requestWaiver` calls `requiredApprovers("WAIVER", "INR", amount, project_id)` at REQUEST time and ships with zero seeded WAIVER bands, so a waiver cannot even be requested until Policy Studio configures one — deliberately not seeded to fake a green happy path (advisor-mandated, `waivers.test.ts` pins the fail-closed case explicitly before the configured-band case).
- Rule 9 (financial clearance): `financial-clearance.ts` reuses `finance.ts`'s existing `bookingFinance()` (H7) for the money side rather than reimplementing it; adds the checklist + approve/reject/immutability persistence on top. `CLEARANCE_APPROVER_ROLES` = Policy Studio roles + ACCOUNTS.
- `routes-collections.ts` + `server.ts` wiring for all of the above. Explicitly NOT built, per the spec's own dependency list: payment-plan Studio CRUD (only one plan per project exists today, nothing to select between — `payment_plan` isn't in 25's `TABLE_REGISTRY`), ageing-band rollup, `/statement` (needs 22, document rendering).
- **Advisor review before merge caught two real bugs, both fixed**: (1) the DISPUTED-receipt exclusion had only been added to `DEMAND_SELECT` and the sweep's own query — `finance.ts`'s `bookingFinance` (which feeds `financial-clearance.ts`'s hard REGISTRATION/HANDOVER gate) and `collections-view.ts`'s `t2Payments` (the customer-facing paid total) both still summed disputed receipts as paid. Fixed all three call sites, added a regression test asserting `bookingFinance().paid` drops when a receipt is disputed. (2) `approveWaiver` mutated `demand.amount` directly, which broke `demands.test.ts`'s existing invariant that a booking's demand schedule sums to `total_consideration` — passed only because `waivers.test.ts` runs against different bookings in its own DB. Fixed to leave `amount` untouched and subtract approved waivers inside `DEMAND_SELECT`'s (and the sweep's) `remaining` subquery instead, same shape as the dispute exclusion; the `waiver` row stays the durable record of why `remaining` shrank. Also fixed on the same pass: `checklistBlockers` iterated the stored jsonb's own keys (`Object.entries(checklist)`), so a key missing from an older row would silently never block — switched to iterating `DEFAULT_CHECKLIST`'s keys so a missing key fails closed, matching the matrix's own fail-closed discipline; and the sweep's dedup guard (matched on `source_entity_id` + `type` alone) could theoretically collide with `setOverdueReason`'s own follow-up action if a reason is ever cleared and re-swept — keyed it on the reminder's own title too.
- **Deliberate, logged conflict left unresolved for Amarsh, not silently decided either way**: rule 4 says a receipt against a SCHEDULED demand should default-reject (advance only, needs an explicit answer) — but `demands-schedule.test.ts` already has a real, merged, tested case ("a receipt against a scheduled demand never vanishes from collections") relying on early payments working today. Recognized as a genuine premise conflict between the spec's proposed default and shipped, deliberate behavior; left `postReceipt` unchanged rather than break a passing test or silently pick a side — needs Amarsh's actual answer to the spec's own client question.
- Other logged scope cuts: receipts default to VERIFIED at post time (no bank-reconciliation step exists, so there's no real PENDING queue yet); `bank_disbursement_applicable` stays a manual checklist fact (auto-derivation needs 21, loans, not built).
- Verified: tsc clean, 68/68 API test files, 397/397 tests (7 new files this slice: `collections-view.test.ts`, `money.test.ts`, `demands-receipts-verify.test.ts`, `collections-sweep.test.ts`, `tds.test.ts`, `waivers.test.ts`, `financial-clearance.test.ts`, plus one new `describe` block in `demands.test.ts`). No frontend changes — collections dashboard/workbench UI deferred, same reasoning as every other R2/R3 backend slice so far.

## 19:40 IST — discovered `docs/reports/2026-09-05-branch-review.md` on `main`; pausing R3 spec sequence to surface it to Amarsh

- On `git pull` after merging PR #25, `main` carried an extra commit (`afb00ec`, pushed directly, not via PR — landed on `main` sometime between this session's spec-19 branch point and its merge) not authored by this session: a consolidation brief written by an agent running on Vivek's laptop, addressed to "Amarsh's agent." It documents that Vivek independently built a parallel HomeFlow backend on Python/FastAPI + Postgres with row-level security, Google OIDC identity, and CDK infra over the same 24 hours, pushed as `vivek/v1-on-postgres`, and recommends porting 6 pieces of it (RLS + app role, compose Postgres for local dev, Google OIDC/OTP, CDK infra lockdown, actor-attributed events, stack-neutral spec content) into this TypeScript branch as R3's *first* priority — ahead of further business-logic specs — plus flags a live security finding: this session's own provisioned RDS instance is `--publicly-accessible` with a `0.0.0.0/0` ingress rule on port 5432 (`infra/scripts/provision.sh`).
- Not actioned by this session: the brief names decisions only Amarsh (with Vivek and Rambabu) can make — ownership split, whether/when to port the RLS foundation, the AWS account/data question, and the exposed-database fix itself all touch CI/infra and a second person's work, both outside this session's standing authorization to decide unilaterally. Stopped the autonomous R3 spec sequence (was about to start 21-loans) to ask Amarsh directly rather than silently continuing past this or silently starting the consolidation procedure the brief prescribes.

## 20:05 IST — consolidation P1: row-level security merged (0025_rls.sql) — first port from the branch-review brief

- Amarsh's answer to the pause above (AskUserQuestion): "Pause and start the consolidation procedure" — the brief's section 8, ported in the order it names, own PRs, own Definition of Done. Branched `consolidate` from `main`. Read all 6 background documents first (this repo's 00-conventions.md/SCHEMA.md/01-identity-access.md/02-event-log.md/03-platform-deploy.md, plus Vivek's 02-database.md/03-identity.md technical specs pulled via `git show origin/vivek/v1-on-postgres:...`), and confirmed Docker is available — all orientation, no code yet.
- Before writing any RLS SQL, empirically probed PGlite rather than trusting the brief's own G2 finding ("RLS can't be exercised locally, PGlite runs as a superuser"). A throwaway script (CREATE ROLE homeflow_app LOGIN NOBYPASSRLS, ENABLE/FORCE ROW LEVEL SECURITY, a policy, then SET ROLE homeflow_app + set_config) proved PGlite's non-superuser SET ROLE genuinely enforces RLS in its single-user WASM mode — scoped SELECT returned only the in-scope row, RESET ROLE (superuser) saw everything, and an unset app.realm returned zero rows (fail-closed). A second probe covered what the first hid: GRANT ... ON ALL TABLES IN SCHEMA public + ALTER DEFAULT PRIVILEGES both work and a table created after the default-privileges statement inherits the grant with no explicit GRANT; and a bare USING clause (no WITH CHECK) already rejects an out-of-scope INSERT — Postgres's documented behavior of using USING as the check when WITH CHECK is omitted, confirmed working in PGlite too. This is the best of the three outcomes an advisor pre-check had flagged as live possibilities, and it changes P1's shape materially: P2 (Postgres-in-Docker) is no longer a prerequisite for P1, and the sweep test lives in ordinary Vitest against PGlite, not a separate Postgres-gated suite.
- Enumerated every table in every merged migration (0000_init.sql through 0024_collections.sql, ~65 tables) by hand — which carry a direct project_id column (NOT NULL, or nullable-meaning-global) versus which don't. Wrote 0025_rls.sql: homeflow_app role + blanket grants/default-privileges; the project table itself scoped by id; a DO-block loop enabling RLS + a project_scope policy on 16 NOT-NULL-project_id tables (unit, booking, demand, receipt, collection_policy, handover_policy, generated_document, registration_case, snag, handover_record, dlp_window, home_passport_item, warranty_case, intervention, project_hierarchy_node, journey_instance); a second loop for 6 nullable-project_id tables where NULL means "global row, visible to any scoped staff" (payment_plan, document_template, event, journey_template, action, approval_authority_rule). The migration's own header comment carries the full exclusion audit (identity/principal-loading tables — chicken-and-egg, they're how project_ids gets resolved before GUCs exist; global config/registry tables; join-reachable tables with no direct project_id like waiver/tds_record/financial_clearance via booking_id, deferred as P1b work rather than denormalizing a schema change without Amarsh's sign-off).
- src/rls.test.ts: seeds a second minimal project directly (superuser, bypasses RLS by definition) alongside the existing rich p_eastcrest demo seed, then SET ROLE homeflow_app + set_config and asserts — across a representative sample (project, unit, booking, demand, receipt, snag, collection_policy, event, action), not literally all ~22 RLS-enabled tables, and the test file's own header says so rather than letting a "397 tests passed" line imply broader coverage than it has — zero cross-project rows, all_projects bypass matches the superuser count, fail-closed on empty app.realm, INSERT into an out-of-scope project rejected, INSERT into the connection's own project accepted.
- Deliberate split from the brief's single P1 row, logged not silent: this PR does not thread GUCs into any real request path. pg-adapter.ts's transaction() still doesn't SET LOCAL app.* from ctx.actor — an advisor review flagged that most reads in this codebase are bare db.query, not always inside a transaction, so "wire it into transaction()" (the brief's literal suggestion) under-scopes the real work; threading needs an AsyncLocalStorage approach plus a named runAsSystem() escape hatch for migrations/seed/subscribers, which is real, separate, reviewable work (P1b, its own PR, its own advisor pass). Until P1b lands, 0025_rls.sql is inert for all ~100 existing call sites (superuser bypasses RLS) — intentional, not a gap being hidden.
- Reordered the brief's own sequence: P1 → P5 (actor-attributed events, small) → P3 (Google OIDC/OTP, additive to email/password, needs an openid-client dependency ask) → P2 (Postgres-in-Docker, now a parity check rather than a prerequisite). P4 (CDK/live-RDS lockdown) and P6 (fold Vivek's spec content) stay deferred per the brief's own instruction until Amarsh answers section 10's account questions — the public-RDS-ingress exposure is still open, not fixed by this PR.
- Verified: npx tsc --noEmit clean; full backend suite 69/69 files, 404/404 tests (up from 68/397 at spec 19's merge — 7 new in rls.test.ts); migration applies cleanly end-to-end against a fresh PGlite instance standalone, and the existing suite is unaffected (RLS is inert under superuser, as designed).

## 20:24 IST — consolidation P5: actor-attributed events merged — second port from the branch-review brief

- Closes R0.6c (logged as a real gap in SCHEMA.md's drift #5 during R0.5: every `appendEvent()` call site stamps `actor_kind='SYSTEM'`, `actor_user_id=null` regardless of who actually acted, even though `ctx.actor` has been available inside every handler since R0.6). `events/append.ts` now exports `actorFields(ctx): Pick<EventInput, "actor_user_id" | "actor_kind">`, mapping `ctx.actor.kind` (`STAFF`→`USER`, `CUSTOMER`→`CUSTOMER`) plus `ctx.actor.user_id`. Every one of the ~65 production `appendEvent()` call sites across 23 files (`bookings.ts`, `bookings-crm.ts`, `demands.ts`, `demands-receipts.ts`, `demands-schedule.ts`, `handlers.ts`, `legal-docs.ts`, `qa.ts`, `warranty.ts`, `tower-view.ts`, `journey/instances.ts`, `journey/templates.ts`, `model/applicants.ts`, `model/bookings.ts`, `model/customers.ts`, `model/units.ts`, `projects.ts`, `actions/core.ts`, `approvals/matrix.ts`, `financial-clearance.ts`, `studio/core.ts`, `tds.ts`, `waivers.ts`) now spreads `...actorFields(ctx)` into its event payload.
- Three functions had no `ctx` reaching their `appendEvent` call and needed a signature change to carry one through: `setupFunding`/`raiseDemandsForUnit` (`demands-schedule.ts`) gained a `ctx` parameter (all call sites updated); `insertUnit` (`model/units.ts`) gained an optional `actor` parameter, since it's also called from seed code with no real actor.
- The one real async-attribution case: `journey/subscribers.ts`'s post-commit `sales_handover.accepted → journey.instantiate` subscriber runs after the triggering transaction has committed, so it has no live `ctx` at all. Rather than default this to SYSTEM (technically correct — no request is in flight — but misleading, since the journey instantiation is causally 100% attributable to whoever accepted the handover), it forwards the *triggering event's own* `actor_user_id`/`actor_kind` into `instantiateJourneyForBooking`'s new optional `actor` parameter, so `journey.started` still attributes to the real acting user.
- New `events/actor-attribution.test.ts`: two unit tests pin `actorFields()`'s STAFF→USER / CUSTOMER→CUSTOMER mapping directly; two integration tests drive real handlers (`createBooking` + `acceptBooking`) end to end and assert every event they emit — including `booking.created`, `customer.created`, `sales_handover.accepted`, `booking.status_changed`, both `unit.sale_status_changed` events, and critically the post-commit `journey.started` fired by the subscriber — carries the real acting user's id and `USER` kind, never SYSTEM or null.
- Mid-slice self-caught mistake, logged per this session's own git-safety discipline: while confirming the current branch before creating a dedicated `p5-actor-events` branch, ran `git checkout main -- .` without first checking `git status`, which discarded all 23 files' uncommitted P5 edits (the new untracked test file was unaffected; P1's already-merged history was unaffected). Detected immediately via `git status --short` and a `grep -c actorFields` sweep returning 0. Redid every transformation from scratch identically (verified via the same grep sweep, plus a full tsc + suite re-run matching the pre-accident numbers exactly) rather than reconstructing partially or guessing. No data or shipped code was lost — only in-progress uncommitted work, fully recovered.
- Verified: npx tsc --noEmit clean; full backend suite 70/70 files, 408/408 tests (up from 69/404 at P1 — 1 new file, 4 new tests).

## 20:34 IST — consolidation P2: Postgres-in-Docker parity check — confirms P1's RLS finding on real Postgres

- Advisor-recommended next step over P3: P3 needs a new dependency ask (`openid-client`) plus a real OAuth client Amarsh hasn't supplied yet (TODO's open question #2), so it can't reach Definition of Done unattended — P2 has neither blocker and directly verifies P1's central empirical claim before P1b threads GUCs into real request paths.
- Added `docker-compose.yml` at repo root: a single `postgres:16` service (host port 5433, named volume, healthcheck), deliberately scoped to just Postgres — this stack's other ports (files, mailer, LLM) already have non-Docker local adapters (local-disk, `.eml`, fake), so Vivek's reference compose's `minio`/`mailpit`/`createbuckets` services don't apply here and weren't ported. Single superuser connection (no separate owner/app-role split) to match the existing `DATABASE_URL` routing in `db/index.ts`, which connects as one user and lets `0025_rls.sql`'s own `CREATE ROLE homeflow_app` mint the scoped runtime role via migration — same as it will against RDS.
- Ran `npm run migrate` with `DATABASE_URL` pointed at the container: all 11 migrations (`0000_init.sql` through `0025_rls.sql`) applied cleanly against a fresh real-Postgres database, zero code changes needed to `db/index.ts` (as expected — the adapter routing already branches on `DATABASE_URL`'s presence).
- **Parity check**: manually replicated `rls.test.ts`'s five core assertions directly via `psql` against the running container (seeded two minimal projects/units by hand, then `SET ROLE homeflow_app` + `set_config`) — scoped SELECT returns only the in-scope unit, an empty `app.realm` returns zero rows (fail-closed), `app.all_projects=true` returns both, an INSERT into an out-of-scope project is rejected with a real `new row violates row-level security policy` error, and an INSERT into the connection's own project succeeds. All five matched PGlite's behavior from `rls.test.ts` exactly. This confirms P1's empirical finding (PGlite's `SET ROLE` genuinely enforces RLS, contradicting the branch-review brief's own G2 claim) generalizes to real Postgres — not just an artifact of PGlite's WASM single-connection mode. No correction needed to `0025_rls.sql`.
- Verification container torn down (`docker compose down -v`) afterward — the fixture rows (`p_parity_a`/`p_parity_b`) were throwaway, not meant to persist as local dev seed data. `docker-compose.yml` itself is not exercised by CI (Docker availability there is not assumed) — it's a real, working local-dev option going forward, not yet wired into any automated check.
- Drafted (not sent — queued locally for Amarsh's review) a `SendFeedback` note about the `git checkout main -- .` mistake logged in the P5 entry above, now that recovery is stable. Also flagging to Amarsh directly, bundled in one message rather than blocking on it: P3 needs (1) a yes/no on adding `openid-client` as a dependency, and (2) real Google OAuth client credentials — both from TODO's open questions, neither answerable by this session alone.
- **Advisor follow-up, same slice**: the check above only exercised Group A tables (`unit`, `project_hierarchy_node` — both NOT NULL `project_id`). Re-ran the container specifically for a Group B table (`event`, nullable `project_id`, where NULL means "global row visible to any scoped staff") — seeded one event per test project plus one with `project_id = NULL`, then confirmed a `p_parity_a`-scoped connection sees its own event *and* the NULL/global one but not `p_parity_b`'s, and an empty `app.realm` still fails closed even for the NULL row. Matches `rls.test.ts`'s `event`/`action` assertions on PGlite exactly — both RLS policy shapes (Group A's plain equality, Group B's `project_id IS NULL OR ...`) are now proven, not just Group A, on real Postgres. Also added the local-Postgres `DATABASE_URL` line to `.env.example` (previously only in this run-log's prose, not where a dev would actually look).

## 21:15 IST — checkpoint: P1b, P3, P4, P6 deliberately parked by Amarsh's own call; resuming R3

- With P1/P5/P2 merged and verified (69→70→70 test files across the three, all green on fresh `main`), surfaced two things to Amarsh directly rather than only logging them: the still-open live RDS public-ingress exposure (0.0.0.0/0 on 5432, flagged since 19:40 IST, fixed by the still-deferred P4), and P3's two blockers (an `openid-client` dependency ask, a real OAuth client).
- Asked Amarsh explicitly (`AskUserQuestion`) how to proceed with P1b — threading `ctx.actor` GUCs into every query so `0025_rls.sql`'s policies engage on real request paths, not just `rls.test.ts`'s manual `SET ROLE`. Framed honestly as the largest, riskiest single change in this consolidation: ~100 production call sites plus ~70 test files would need updating to pass a scoped actor or an explicit `runAsSystem()` escape hatch, opt-in and fail-closed by design (a missed call site returns zero rows, not unrestricted data) — a materially bigger blast radius than P1/P5/P2, and CLAUDE.md-adjacent to the "ask first: DB schema/migrations, CI/infra changes" boundary even though it isn't literally a migration.
- **Amarsh's answer: hold off, return to R3 business specs.** Not this session choosing to defer P1b unilaterally — his explicit call. `0025_rls.sql` stays inert (superuser bypass, as it has been since P1 merged) but harmless. P1b, P3, P4, P6 all now share the same "deliberately parked, not forgotten" status in TODO.md's Position line.
- Resuming the R3 spec sequence paused at 19:40 IST for the branch-review discovery: next is spec 21 (loans).

## 21:47 IST — R3: home loans backend merged (21-loans.md)

- `loan_case` (real table since `0000_init.sql`: `id, booking_id, lender, sanctioned_amount, status`) cleanly RENAMEd — `lender`→`lender_name`, `sanctioned_amount`→`sanctioned_amount_inr`, `status`→`stage` (richer enum, replacing the old free-text default) — rather than ALTER-alongside like 07/08's own collisions, since only `seed.ts` referenced the old names and no deployment carries real loan data yet (this session's own R2.5/R3 work hasn't deployed anywhere). Logged in `docs/specs/SCHEMA.md`'s drift log (#6) and 21's own Data section. Migration renumbered `0026_loans.sql` (spec names `0019`) since it depends on already-merged `0009_actions.sql`/`0024_collections.sql`/`0025_rls.sql`.
- Built `loans/risk.ts` (pure, framework-free, mirrors `collections.ts`'s `classifyOpenAmount`/`recoveryProbability` style — named drivers, not a hidden model), `loans/core.ts` (create/patch/record-event/documents/list/get + the disbursement waterfall), `loans/sweep.ts` (validity-expiry + gap-breach sweeps, same no-scheduler-exists pattern already documented for 06/19 — directly callable with a controlled `asOf`, exercised only by tests), `routes-loans.ts`.
- Rule 1: creating a loan case flips 19's `bank_disbursement_applicable=true` and marks every open demand on the booking `loan_dependent=true`; REJECTED/WITHDRAWN reverses both and raises a CRM follow-up action. Simplified the spec's "for the sanctioned share" to all-or-nothing (no partial per-demand loan-coverage allocation exists anywhere in this schema to build against) — flagged, not silently guessed.
- Rule 2: disbursement waterfall allocates a single DISBURSED amount across the booking's loan-dependent, not-yet-settled demands in due-date order, creating one `LOAN_DISBURSEMENT` receipt per demand touched (reusing 19's "early payment against a scheduled demand is allowed" precedent), ≤ sanctioned + 1% tolerance, landing PARTIALLY/FULLY_DISBURSED. Deliberately reimplemented the minimal receipt-insert logic directly here rather than calling 19's `postReceipt()` — that requires `collections` WRITE, which Banking doesn't hold.
- Rule 3/5: `getLoanRisk` computes `days_to_demand`/`days_to_disbursement` from real facts (next unsettled loan-dependent demand's due_date vs `expected_disbursement_date`) and feeds `risk.ts`'s pure scorer (stage age, missing docs count, validity days left, lender responsiveness, timing gap) — not 14's ScoreCard contract, since 14 isn't built.
- Rule 4: `sweepLoanValidity` raises a Banking `createAction` (stand-in for 12's unbuilt escalation) when validity is within the warning window; expired flips stage to DOCS_PENDING with blocker "Sanction expired".
- Rule 6: `sweepLoanGapBreach` flips `loan_dependent=false` once a timing gap breaches >15d — zero new bucket-computation code needed, since `collections.ts`'s existing `classifyOpenAmount` already routes a non-loan-dependent, overdue, low-recovery demand to TRUE_RISK.
- Caught a real non-linear-stage bug while writing `recordLoanEvent`: a naive "DOCS_REQUESTED always moves stage forward" check would have blocked rule 4's own expiry path (SANCTIONED → DOCS_PENDING is a legitimate backward-looking move in enum-declaration order). Rewrote the two stage-advancing event handlers (DOCS_REQUESTED, DISBURSEMENT_REQUESTED) to test what each event actually means rather than a generic ordering table — pinned with 3 dedicated tests including one that a loan already disbursing never regresses on DOCS_REQUESTED.
- Added `loan.withdrawn` as a sanctioned event-registry extension — the spec's Events list names only `loan.rejected`, which would have recorded a false audit trail for a withdrawal (same class of gap `waivers.ts` already found and fixed for its own approved/rejected split).
- **Advisor review before merge caught a real authorization error, fixed before it ever reached `main`**: an earlier working-tree edit (never committed — confirmed via `git diff`, zero change registered against the last committed `permissions.ts`) had reasoned that §6's `routers/loans.py` finding (writers = {BANKING, ACCOUNTS, MANAGEMENT}) was more authoritative than the seeded `permission_matrix`'s narrower §1.3 row (BANKING-only WRITE), and widened `seed/permissions.ts` to match. Wrong direction: `seed/permissions.ts`'s own header states the matrix **is** §1.3 verbatim — it's the seeded artifact, not a summary of it, so editing it to match a different source (§6) was resolving a spec-vs-spec conflict unilaterally, the exact class of decision this session has escalated to Amarsh three times already (`postReceipt`/SCHEDULED, the `solid`/`primary` button color, P1b). Reverted the matrix to `R N R R W R R N N` (BANKING-only WRITE); rewrote rule 7's test to assert what the real matrix enforces (ACCOUNTS/MANAGEMENT/CRM read-only, SALES no access, only BANKING writes) and switched every `patchLoanCase` test call from an ACCOUNTS ctx to BANKING to match. Logged the §1.3-vs-§6 conflict as an open question for Amarsh in TODO.md rather than picking a side — same treatment as `postReceipt`. Also flagged (advisor-caught, not previously noted): `loan_case` carries a real `project_id` but no RLS policy, since its migration lands after `0025_rls.sql` — noted in TODO.md's P1b row for whenever that resumes.
- Verified: `npx tsc --noEmit` clean; full backend suite 72/72 files, 436/436 tests (up from 70/408 at P2's merge — 28 new: 7 in `loans/risk.test.ts`, 21 in `loans/core.test.ts`). One full-suite run flagged 4 files with the known pre-existing `Hook timed out`/`Worker exited unexpectedly` PGlite-under-parallel-load flakiness (already documented for 10/19); confirmed not a regression by re-running those 4 files alone (35/35 green) and the full suite twice more (both 72/72, 436/436, clean).
- No frontend changes this slice — Loans pipeline/case-view UI deferred, same reasoning as every other R2/R3 backend slice so far. Next: 20 (cash forecast).

## 22:20 IST — R3: escalations & notifications backend merged (12-escalations-notifications.md)

- Sequencing decision (surfaced via `AskUserQuestion`, ~21:55 IST): 20 (cash forecast) is next in R3's own row order, but depends on 23 (registration, sequenced in R6, three waves out) — building it now would mean stubbing 5 separate things at once. Amarsh chose 12 → 13 → 14 instead; this entry closes 12.
- `0027_escalations.sql`: `escalation_ladder`, `escalation_rule` (13 named rows from the spec's Data table), `escalation` (unique partial index on `(rule_key, source_entity_id)` for open rows — idempotent raise), `notification`, `notification_preference`, `materiality_threshold`, plus `sla_policy.escalation_ladder_id` FK (verified safe first — grepped and confirmed that column was never set anywhere before this migration).
- Built `escalations/core.ts` (`scanEscalations`/`listEscalations`/`getEscalation`/acknowledge-start-resolve-close-reopen via a shared `transition()` state machine/`assertCanAct`/`resolveTierOwner`/`resolveImpact`/`matchRule`) + `escalations/subscribers.ts`, `notifications/core.ts` (`createNotification`/`listNotifications`/`markNotificationRead`/preferences/`sendDigest`) + `notifications/subscribers.ts`, `routes-escalations.ts`, `routes-notifications.ts`, `seed/escalation-rules.ts`.
- **The load-bearing finding, verified not assumed**: exhaustively grepped every `createAction` call site in the codebase (`collections-sweep.ts`, `demands.ts`, `loans/sweep.ts`, `loans/core.ts`, `journey/instances.ts`) for anything setting `due_at`. None do — `journey/instances.ts`'s own comment confirms `action.due_at` is deliberately never duplicated there either; the one place a real deadline lives is `sla_clock.due_at`, reached via `action.sla_clock_id`, set only by `journey/instances.ts`. First draft of `scannableActions` gated on `due_at IS NOT NULL` with a fallback lead-time constant — reverted once this was confirmed, since it was built on the wrong premise. Consequence: `scannableActions` gates strictly on `sla_clock_id IS NOT NULL`, and all 13 named `escalation_rule` rows ship `wired=false` — none have a deadline-bearing action to escalate through today (commitment/13, snag/15, handover/16, registration/23 are genuinely unbuilt; but payment/TDS/loan-sanction/legal-review's specs (19/21) ARE built — their reminder actions simply carry no due date). `matchRule`/`resolveImpact`'s collections/loans branches are dead by the same reasoning — kept live in code, not exercised. The unlock is a `due_at` on those four call sites' created actions, outside this spec's Files list — named here, not built around.
- Rule 1: `scanEscalations` reuses `deriveStatus` (06's pure engine, not re-derived) to find DUE_SOON/OVERDUE sla_clock-backed actions, tiers by elapsed hours since first raised against the seeded STANDARD ladder (L0/0h/OWNER → L1/48h/BACKUP_OWNER → L2/120h/DEPT_HEAD → L3/240h/PROJECT_HEAD → L4/360h/MANAGEMENT — all `UNCONFIRMED` placeholders, **ASK_CLIENT**, p22 gives no real hours), resolves the tier's target owner through `project_team_assignment`'s previously-unused `escalation_manager_user_id`/`is_backup_owner`/`role_scope` columns (no multi-level org chart exists beyond one escalation-manager pointer, so DEPT_HEAD/PROJECT_HEAD/MANAGEMENT all resolve through the same chain), builds a `decision_pack` (blocked_what/since/impact/options/recommended/owner_history), and raises/updates the `escalation` row idempotently.
- Rule 3: auto-resolve needed a real-time path, not just the next sweep — `scannableActions`'s own query excludes closed actions/stopped clocks by construction, so a periodic sweep alone could never revisit and resolve an escalation whose action had since closed. `escalations/subscribers.ts` subscribes to `action.closed`/`action.cancelled` (both real, already `built:true`) to resolve immediately.
- Rule 4: `listEscalations` filters by `materiality_threshold` only for MANAGEMENT/SUPER_ADMIN viewers — everyone else sees the unfiltered list. Test proves the filter is actually live (not vacuously permissive): flips one escalation's `decision_pack.impact` below both seeded thresholds mid-test and confirms MANAGEMENT loses it while a non-MANAGEMENT viewer still sees it — caught in advisor review before merge, since the first draft's seeded `CUSTOMER_COUNT: 1` threshold happened to pass every escalation, so the original assertion would have passed identically with no materiality logic at all.
- Rule 7: matrix WRITE is CRM-only (§1.3, narrow — same "trust the seeded matrix" discipline 21-loans.md's revert established), layered with a per-instance self-guard: the escalation's own current `owner_user_id` may also act on it even without matrix WRITE, mirroring `actions/core.ts`'s existing `submitted_by` self-guard — otherwise a BANKING-owned L2 escalation could never be acted on by the BANKING dept head it was just tiered to.
- Rule 5: `createNotification` always writes the IN_APP row; emails via the real `mailer` port only when the preference's `email_on` level matches and it's outside quiet hours (`isWithinQuietHours` is wrap-midnight-safe via `clock.nowIst()`). `sendDigest` counts unread notifications + open owned escalations, emails a summary if non-zero.
- **Real bug caught pre-merge, not by a user report**: `resolveEscalation` was a non-`async` arrow function that threw synchronously before its first `await` on empty `resolution_notes` — meaning `.rejects.toThrow()` assertions would have seen a normal thrown error instead of a rejected promise (the route's try/catch would have masked it in production too). Fixed by converting to `export async function`. Checked the other 4 transition exports (`acknowledgeEscalation`/`startEscalation`/`closeEscalation`/`reopenEscalation`) and `loans/core.ts` for the same shape — none have a throw before their first `await`; this was an isolated case, not a pattern needing a broader sweep.
- No scheduler exists anywhere in this codebase (same gap already documented for 06/19/21) — `scanEscalations`/`sendDigest` are directly callable with a controlled `asOf`, tested, not cron-wired.
- Verified: `npx tsc --noEmit` clean; full backend suite 74/74 files, 448/448 tests (up from 72/436 at 21's merge — 12 new: 6 `escalations/core.test.ts`, 6 `notifications/core.test.ts`). One full-suite run flagged `rls.test.ts` with the same pre-existing `Hook timed out` PGlite-under-parallel-load flakiness already documented for 10/19/21; confirmed not a regression by re-running it alone (7/7 green) and the full suite once more (74/74, 448/448, clean).
- No frontend changes this slice — same reasoning as every other R2/R3 backend slice so far. Next: 13 (promise ledger).

## 22:40 IST — R3: promise ledger backend merged (13-promise-ledger.md)

- `0028_commitments.sql` (`commitment`, `commitment_transition`), `commitments/core.ts` (full DRAFT→APPROVED→ACTIVE→AT_RISK→FULFILLED/BREACHED/WAIVED_CANCELLED lifecycle + `scanCommitments` sweep), `commitments/confidence.ts` (pure scorer), `routes-commitments.ts`. 21 new tests, tsc clean, full suite 76/76 files, 466/466 tests (up from 74/448 at 12's merge).
- Real win: rule 8's handover gate is genuinely wired — `handover.ts`'s `open_commitments` input + `qa.ts`'s `handoverForBooking` real query, replacing the permanent "No commitment records — Promise Ledger not yet built" stub (PR #7's "Not verified" closed for real).
- Corrected a wrong premise while wiring the gate: the stub's own comment claimed "gates.md classifies commitments as hard" — checked gates.md §B.2 directly; its hard-gate list is {financial, legal, registration, critical-snag, safety-physical}, commitments isn't in it. Kept the gate soft, logged the correction rather than carrying the old claim forward.
- Real scope cuts, flagged not faked: rule 2's approver resolution is a direct in-code default rather than `approvals/matrix.ts`'s `requiredApprovers("COMMITMENT",...)` — that lookup fails closed with zero seeded rows, which would block every approval until Policy Studio is configured; named as the real upgrade once a band exists. Rule 6 (auto-create from a sales handover packet) has no real source since 17 isn't built. Rule 5's `depends_on` only resolves ACTION/DEMAND facts for real (CHANGE_REQUEST/PROGRESS score neutral). Rule 7 analytics deferred (no consumer, 27, exists yet).
- `commitment` carries project_id but no RLS policy — same flagged gap as loan_case (21)/escalation (12); sweep all three together when P1b resumes.
- No frontend changes this slice. Next: 14 (readiness scores).

## 22:56 IST — R3: readiness scores backend merged (14-readiness-scores.md) — R3 complete

- `0029_scores.sql`, `scores/contract.ts` (shared Score type), `scores/{unit,booking,handover}-readiness.ts`, `scores/store.ts`, `routes-scores.ts`. 3 new tests, tsc clean, full suite 77/77 files, 469/469 tests.
- Unit readiness reuses the existing binary QA-verified engine (flagged: not rule 1's full weighted state model, needs 07). Booking readiness is a real weighted blend (payments/TDS/loan/agreement/registration/waiting-customer-actions); documents component flagged unavailable (17/22). Handover readiness reuses `evaluateHandover` with a real commitment-count penalty (13); FM component flagged unavailable (16).
- Score weights are in-code constants, not read from `score_weight` (schema exists, unwired — Studio can populate it later).
- R3 (CFO-first) is now fully backend-complete: 19, 21, 12, 13, 14 all merged. 20 (cash forecast) remains deliberately skipped pending 23/registration or Amarsh's sign-off to stub it.

## 23:06 IST — checkpoint: R3 complete, moving to R4 (Amarsh's call)

- Amarsh asked to speed up the whole build. Agreed cuts going forward: skip fresh-main re-verify after a clean fast-forward merge, leaner TODO/run-log entries (facts, not narrative), leaner tests (one per rule instead of exhaustive variations).
- R4 order picked by real dependency count, not spec-list order: 26 (customer portal) and 27 (control tower) both depend on many unbuilt R5/R6 specs (07/15/16/18/23/30) — same "wrong next item" class as 20. 11 (my day) has zero forward dependencies (only 10/06/01, all built) — built first.

## 23:12 IST — R4: my day backend merged (11-my-day-ranking.md)

- `myday/rank.ts` (pure 5-term weighted scorer + tie-break + `whyNow`), `myday/core.ts` (`getMyDay`/`getTeamDay`), `routes-myday.ts`. 10 new tests, tsc clean, full suite 79/79 files, 479/479 tests.
- Reused 12/21's own finding: `action.due_at` is never set outside `journey/instances.ts` — due dates read from `sla_clock.due_at` instead.
- Found two more dead pre-declared columns: `action.impact` (jsonb, literally commented "for 11, not consumed here") and `action.escalation_tier` (always 'L0') — neither is ever written by any code path. Derived the same facts live via real joins instead (booking revenue/customer count, `depends_on_action_id` count, 12's `escalation` table).
- Team view (rule 5) is project-scoped, not true team-hierarchy — flagged, no per-team roster query exists yet.
- Next: 17 (sales-CRM handover) or 22 (document factory) — both mostly unblocked.

## 23:54 IST — R4: sales → CRM handover backend merged (17-sales-crm-handover.md)

- `0030_sales_handover.sql` (`sales_handover`, `handover_checklist_rule`, `return_reason`), `seed/handover-checklist.ts` (20 standard rules + 9 return reasons), `sales-handover/checklist.ts` (pure resolve + weighted score), `sales-handover/core.ts` (submit / accept / return / metrics / queue), `routes-sales-handover.ts`. 5 new tests covering rules 1–7, tsc clean, full suite 80/80 files, 484/484 tests.
- Built additively, not the spec's "replace bookings.ts": measured first — 16 test files import `acceptBooking`/`createBooking`. `acceptHandover`/`returnHandover` delegate to them and layer the packet rules on top. Zero regressions.
- Rule 5's `booking.status = CRM_ACCEPTED` deliberately not applied — 5 read sites filter `status = 'active'`, nothing moves crm_accepted→active. `sales_handover.status` carries the feature. Documented in `model/status.ts`.
- 13 rule 6 now real: `createCommitmentFromSource(input, ctx, tx)` added to `commitments/core.ts` (Sales holds only READ on `commitments`, so the packet can't use the ctx-gated `createCommitment`); accept approves+activates the DRAFTs.
- Found: `returnBooking` had no status guard — return-after-accept was silently allowed. Added the `'submitted'` check (only edit to existing accept/return code). `sales_owner_user_id`/`rm_owner_user_id` were never written by anything before this; both now populated.
- Gotcha hit: calling a function that opens its own `withTx` from inside another `withTx` hangs on the single-connection PGlite adapter (5 s timeout). Restructured accept into sequential transactions.
- Journey instantiation came free — 06's existing `sales_handover.accepted` subscriber fires from the unchanged `acceptBooking`. Test asserts `journey_instance` exists after accept.
- Next: 22 (document factory) — re-checking its dependency line before starting.

## 00:09 IST 2026-09-06 — checkpoint: 22 skipped (Amarsh's call), 07 is next

- Surveyed 22 before starting: 8 tables; its `document_template`/`generated_document` redesign (new column names, UPPERCASE statuses) collides with 4 live readers (qa.ts, scores/booking-readiness.ts, transparency.ts, seed-lifecycle.ts) + 4 test files on the current lowercase vocabulary; its Files list says "replace `legal-docs*.ts`" — same instruction class 17 declined. Offered thin slice (rule 8 customer-document checklist) / full factory / skip. Amarsh chose skip. 17's `documents_section` stays on `booking.docs`.
- R5 candidates by real dependency line: 07 needs only 01/02/04 (all built) — the sole unblocked one. 15 needs 07; 16 needs 15/22/23; 18 needs 08/09/15/22/26; 23 needs 22/20. Building 07.
- Known going in (TODO §9): `component_definition` and `unit_progress` already exist in `0000_init.sql` — 07's migration must ALTER, not CREATE; 0006 is taken, so it lands as 0031.

## 00:25 IST 2026-09-06 — R5: unit progress control backend merged (07-unit-progress-control.md) + DB session clock fix

- `0031_progress.sql` ALTERs `unit_progress`/`component_definition` in place (+ new `progress_bulk_update`, `progress_reopen`), `progress/{core,freshness,gate-inputs}.ts`, `routes-progress.ts`; `handlers.ts::setProgress` is now a compatible wrapper over `updateProgress`, so every write path gets rules 1–4/7/8. 9 new tests (rules 1–8), tsc clean, full suite 82/82 files, 493/493 tests.
- Found and fixed at the cause (own commit in PR #36): PGlite's session TimeZone is GMT (RDS is UTC) while the app's `today()` is IST, so every SQL `CURRENT_DATE` — seed demand due dates, `journey/instances.ts` `actual_end`, 17/07 effective-dating reads — was one day behind IST from 00:00–05:30 IST. Surfaced at 00:20 IST as `collections-view.test.ts:40` / `demands.test.ts:174` failing on seed `d_v110_2` (due `CURRENT_DATE` → classified OVERDUE against IST today). Proven with a direct query: `current_date` 2026-09-05 vs `todayIst()` 2026-09-06. Both adapters now pin `SET TIME ZONE 'Asia/Kolkata'` per session (`db/session.ts`). Five JS-side UTC-day lines remain, enumerated in TODO §9 for one follow-up.
- `rework` added to `gates.ts::ProgressState`; the compiler found exactly two `Record<ProgressState, number>` sites (`gates.ts`, `customer.ts`) — both rank it with in_progress.
- Registry coverage test caught a real gap: my rule-6 test asserted the stale sweep's action but not its `progress.stale` event — assertion added, not the test relaxed.
- Next: 15 (QA evidence & snags) — the one spec 07 unblocks directly.

## 00:55 IST 2026-09-06 — R6: QA checklists, evidence & snags backend merged (15-qa-evidence-snags.md)

- `0032_qa.sql`: new `qa_checklist_template`, `qa_inspection`, `qa_inspection_evidence`, `external_dependency`, `contractor`, `snag_sla_policy`; `snag` ALTERed in place (22 columns, lowercase vocabulary kept, Appendix A names at the API); `sla_policy.applies_to` gains `SNAG_SEVERITY`; `handover_policy.major_snag_max`. Modules `qa/{templates,inspections,dependencies,snags}.ts`, `routes-qa.ts`, `seed/qa-templates.ts`. 8 rule tests; suite 83 files / 501 tests, tsc clean.
- Collision handled without a rename: the real `qa_evidence` is the (unit, component) QA-verified flag 14 and the handover gate read — kept; 15's evidence-file table is `qa_inspection_evidence`, and every QA PASS/FAIL upserts the legacy flag so readiness never disagrees with the inspection log.
- Rule 6 wired for real through 12: each snag owns an `exec_simple` action carrying its severity SLA clock, so `scanEscalations` raises the escalation once overdue (tested: CRITICAL due in exactly 2 calendar days, escalation to the assignee after +1 day).
- Rule 1 + 07 together: site declaration → `complete/SITE_ENTRY`, QA fail → `rework/QA_VERIFICATION` + one snag per failed item + SITE re-inspection action, re-inspection pass → `verified`. 07's state write runs as its own transaction after the inspection's (the nested-withTx lesson from 17).
- Found: `component_definition` rows are demo data, so a config seed that FKs onto them fails on a fresh DB before the demo seed — templates now seed after it and skip absent components. Same 07 question stands: components should become config.
- Built to the matrix, flagged: MANAGEMENT can't reopen snags (READ only on `snagging`); `critical_snag_2d` stays unwired (12's rule match has no severity filter).
- Next: 16 (handover gates) — still needs 22/23 for its document and registration inputs; will survey what's genuinely unblocked first.

## 01:05 IST 2026-09-06 — R5: changeability engine backend merged (08-changeability-engine.md)

- Went to 08, not 16: 16 still needs 22 (skipped) and 23 for its LEGAL/REGISTRATION/CUSTOMER gate inputs; 08 needed only 07/02/01, all built, and unblocks 24/18/14.
- `0033_changeability.sql` ALTERs `change_category`/`change_gate_rule` in place (SCHEMA.md drift #4 as written), adds `unit_change_gate`, `unit_gate_exception`, `gate_evaluation_log`. `gates.ts` gains the full pure engine (`evaluateCategory`, `flexibilityScore`) alongside the untouched `deriveGate`; `changeability/{core,subscribers}.ts`, `routes-changeability.ts`. 28-row engine table + 7 rule tests; tsc clean.
- Rule 3 is now a real event loop: a 07 progress write → `progress.updated` → subscriber re-evaluates → `unit_change_gate` flips with `source_event_id` → `gate.state_changed`. Tested end to end through `updateProgress`, not by calling the evaluator directly.
- Found: `scores/store.ts::previousValue` used the module-level `db`; called from inside my transaction it would have deadlocked PGlite — gave it an optional `tx`. Same lesson as 17's nested `withTx`, one layer down: anything that runs inside a transaction must take the transaction.
- Found: seed.ts's demo rules land after migrations, so 0033's `code` back-fill saw an empty table — `COALESCE` in the loader. Second time today a "config" table turned out to be demo data (15's components).
- Flagged: procurement/drawing/slab triggers have no producers; MANAGEMENT-by-default exception authority; publish always needs a reason.
- Next: 24 (sales inventory & discovery) now has 07+08; 18 still needs 09/22/26. Surveying 09 vs 24.

## 01:20 IST 2026-09-06 — R5: sales inventory, discovery, holds & booking backend merged (24-sales-inventory-discovery.md)

- `0034_sales.sql`: `prospect`, `prospect_personalisation_need`, `unit_requirement_match`, `hold_policy`, `change_window_hold`, `sales_policy`; `booking` + prospect/discount/approval/needs columns. `sales/{match,policy,inventory,prospects,holds,booking,subscribers}.ts`, `routes-sales.ts`. 5 pure + 6 rule tests; tsc clean.
- The inventory read model is a composition, not a new store: 04 unit master + 07 progress (construction %) + 08 `evaluateUnit` (flexibility, chips, closing-soon) + 14/qa readiness (ready-to-move) + 24 holds (held-until). Sales writes nothing on physics.
- Hold → gate: an APPROVED Change Window Hold now clamps 08's evaluator for that unit/category (tested: mep_first_fix COMPLETE leaves kitchen_layout OPEN with reason HOLD while electrical closes), 07's bulk preview flags the unit `held`, and `scanHolds` expiry lets the gate close on the next read.
- Booking from inventory is additive next to the pre-24 `createBooking` (17 test files on it). Discount approval reuses 25's matrix and 10's approval action; `payment.received` auto-confirms a DRAFT inventory booking.
- Found: `action.booking_id` FKs `booking` — create the row before its approval action. Found: `booking_applicant.role` mixes `'primary'` with uppercase `CO_APPLICANT`.
- Flagged: possession window bands and hold limits UNCONFIRMED; bathroom filter has no category; CRM can book from inventory (matrix), SITE cannot.
- Next: 09 (specification baselines & revisions) — the last R5 spec with every dependency built (04/03); then 18 needs 09 + 22 + 26.

## 01:45 IST — 09 spec revisions merged (PR #40)

- Specification baseline (DRAFT/APPROVED/RETIRED, versioned per project/product/unit-type scope) attaches at booking confirmation as `spec_revision` 0 (BASELINE, RELEASED) — subscribed to both 24's `booking.status_changed` (DRAFT→CONFIRMED) and the pre-24 `booking.created` (creation IS confirmation on that path).
- DRAFT→RELEASED customisation/as-built revisions supersede the prior current revision (`spec_revision.superseded`, read-only + banner) and emit `drawing.released` with `unit_id` — 08's `observedEvents` already mapped that event name to DRAWING_RELEASED, so the trigger started resolving with zero changes to `changeability/core.ts`.
- As-built recording (`recordAsBuilt`) diffs released vs actual and is a no-op when they match. Drawings upload via the files port onto DRAFT revisions only. Variation catalogue: project row overrides the standard row, same pattern as 08's rules and 24's policies.
- 18's handlers are the intended callers of revision creation/release/as-built — plain `(tx, actor)` functions, no ctx gate, so 18 composes them inside its own transaction (never nests a second `withTx` around them).
- Flagged: `unit.specification_baseline_id` (pre-existing, unused) now kept in sync alongside the real `unit_specification` pointer row; a unit with no APPROVED baseline for its scope gets a named blocker, not a crash, at confirmation time.
- Full suite: 88 files / 554 tests, `tsc --noEmit` clean.
- Next: 18 (change requests) still needs 22 + 26; survey R5/R6 remainder (16 needs 22/23, 23, 26, 27, 30) for the next fully-unblocked spec.

## 02:20 IST — 22 document factory resumed and merged (PR #41)

- Resumed after being deliberately skipped 2026-09-06 00:09 IST for colliding with 4 live readers + 4 test files — surfaced to Amarsh once every remaining R4–R7 spec (16, 18, 23, 26, 27, 29, 30) turned out blocked on it, directly or transitively; he chose "Build 22 now" via AskUserQuestion.
- Built additively: legacy AOS tables (`document_template`/`generated_document`, `legal-docs.ts`) untouched; 22's other 12 document families live on new `doc_factory_template`/`doc_factory_document` tables — same "different entity, keep both" pattern as 15's `qa_evidence`. Blast radius measured with an Explore subagent before writing any code.
- All 10 backend rules built: versioned template/clause approval (05's condition DSL reused for clause selection), a readiness panel scoped to the merge fields a template actually references, readiness-gated generation with frozen `data_snapshot`/redline/supersede-on-regenerate, LOCKED/PARAMETERIZED/NEGOTIABLE_WITH_APPROVAL clauses with segregation-of-duties deviations that now carry forward across regenerations (real bug caught before running any test), the full review→customer-review→execution→final→archive workflow, and the customer document checklist seeded on 17's real `sales_handover.accepted` event.
- Real port gap fixed: `FilesPort` had no way to persist server-generated bytes — added `putBuffer` to the interface and both adapters, the first real caller of `pdf.render()` (Playwright Chromium).
- Flagged: workflow-level rejection renamed to `document.review_rejected` (spec's own Events list reserves `document.rejected` for the checklist family); registry rows for the 4 checklist events flipped from speculative `built:false` to `true` in place rather than duplicated; full findings list in TODO.md §9 and the spec file's own Build note.
- Full suite: 89 files / 564 tests, `tsc --noEmit` clean.
- Next: re-survey R4–R7 (16, 18, 23, 26, 27, 29, 30) now that 22 is built — 18 likely closest, needing only 26 among its remaining deps.

## 03:05 IST — 18 change requests backend merged (PR #42)

- Re-surveyed every remaining R4–R7 spec's own "Depends on / Feeds" line rather than trusting the prior guess: confirmed a real circular knot between 20/23/16/26/27/29/30, and confirmed 18 is the sole exit — its only unbuilt hard dependency (26, customer portal) has rule 5's own fallback text ("portal 26 or signed copy"), so it ships with signed-copy acceptance only.
- `0037_change_requests.sql`: 7 new tables (`change_request` with the full 16-status CHECK, `change_request_item`, `quotation`, `change_request_approval`, `cr_approval_rule` — its own dedicated Policy Studio table, not 25's generic matrix, same precedent 25 already set for 05/06's bespoke versioning — `customisation_policy`, `cr_execution_action`); closes a gap 08's own migration comment had flagged ("18, not built — no FK") by adding the real FK on `unit_gate_exception.change_request_id`.
- All 12 rules built across `change-requests/{store,capture,costing,approvals,quotation,release,execution,cancellation,economics,policy}.ts`: capture never blocks (auto-rejects on HARD_CLOSED, requires an exception on EXCEPTION_ONLY before costing), a value/margin/schedule/freeze approval matrix creating one APPROVAL action per required role, quotation issue/accept with a real Chromium-rendered PDF (direct via the `pdf`/`files` ports, not 22's template machinery — its merge-field context is booking-scoped only), a payment gate backed by a real demand+receipt or an explicit waiver, release creating a real 09 spec revision + execution actions + consuming the 08 exception, QA link (manual — 08 and 07's category vocabularies don't correspond 1:1, same class 15 already flagged) + verify, customer accept, as-built close, withdraw pre-release / cancel-with-abortive-cost post-release, and per-CR profitability.
- Real design gap self-caught before writing any test: nothing in the spec's own transitions ever writes `change_request.exception_id` — added `costing.ts::linkGateException`.
- The nested-`withTx` lesson (17/22) recurred in new code and was caught again before running anything: composing `submitForApproval`/`approveAction`/`rejectAction` inside this spec's own open transaction would hang on single-connection PGlite — fixed by doing the "submitted" transition via direct SQL inside the existing transaction and calling the others as standalone sequential top-level calls.
- Approver ≠ requester enforced at two independent levels: 18's own `cr.raised_by_user_id === ctx.actor.user_id` check (role-independent, checked before any generic action call) plus the generic `approveAction`'s own submitted-by guard.
- Found and fixed a pre-existing test regression from the new FK: `changeability/core.test.ts`'s rule-5 test used a synthetic `"cr_test"` placeholder id — inserted a real minimal booking + change_request row instead. Found and fixed a real spec-text drift: 25's own Tabs summary line was missing "Variation approval matrix" even though 18's own Screens section names it — 25's registry-coverage test treats that line as authoritative.
- Flagged: no PROJECTS_HEAD role exists, rule 4's schedule-impact approver mapped to SITE; rule 9's post-release refund goes through 19's waiver matrix, which fails closed with zero seeded WAIVER bands — cancellation still succeeds, refund is flagged on the event log rather than forced; rule 12 (first CR activates a conditional Customisation journey stage) is 06's own already-documented unwired gap, this build only fires the event.
- Full suite: 90 files / 568 tests, `tsc --noEmit` clean (one run hit unrelated worker-pool resource-contention flakiness, re-verified clean with bounded concurrency).

## 03:55 IST — 23 registration backend merged (PR #43)

- Re-surveyed R4–R7 after 18 and found a real circular dependency: 20 (cash forecast) needs 23's SLOT_BOOKED for its own REGISTRATION_FINAL_DEMAND line; 23 needs 20's clearance-expected-date as one term in its own forecast. Asked the advisor which to break first — 23, because it unblocks both 16 (whose only unbuilt dependency is 23) and 20, and because 23 is the *producer* 20 would otherwise need a stub for, so building 23 first leaves no stub in either direction.
- `0038_registration.sql` ALTERs the pre-existing `registration_case` (0000_init.sql, still read by `legal-docs.ts`'s own simple `completeRegistration`/`qa.ts`/`scores/booking-readiness.ts` keyed on `status='completed'`) with the full rich lifecycle — readiness jsonb (8 keys: the Data table's 7 plus a real addition, `sale_deed_ready`, for a hard input rule 1 names but the Data table's own list omits), slot scheduling + history, day-of checklist, execution/completion fields; new `registration_checklist_template` backs the two pre-existing Studio placeholder tabs.
- All rules built in `registration/{store,readiness,core,policy}.ts`: readiness computed live off 22's checklist/AOS/Sale-Deed, 19's clearance/TDS, and a KYC/POA judgment-call mapping; availability confirm (CRM-on-behalf only, portal 26 not built) → slot booking (requires READY + confirmed availability, full slot_history) → day-of gate → execution → completion (deed FINAL + doc number, sets booking/unit REGISTERED, emits the same `registration.completed` the legacy flow already fires — both producers now coexist on one row, same "different producer, one entity" pattern 15/18 already used).
- Real regression caught by running the full suite twice: two pre-existing raw-SQL `registration_case` inserts (`seed-lifecycle.ts`'s 3 demo fixtures, and — more seriously — `legal-docs.ts`'s own `completeRegistration`, a real production path) broke on the new NOT NULL `code`/`unit_id` columns; fixed all three (plus a third in `scores/core.test.ts`) by minting real codes via `nextCode`.
- Real bug in reusing `documents/checklist.ts::allRequiredAccepted`: it reads zero `customer_document` rows (before 22's checklist is even seeded) as vacuously all-accepted, so a booking that hadn't even reached CRM acceptance read as "documents ready" — caught by this spec's own rule-1 test, fixed in registration's own readiness check rather than the shared helper (22's own callers rely on its current semantics for a genuinely-empty checklist).
- Flagged: rule 4's forecast omits 20's clearance-expected-date term (not built) and has no real source for "document ETA" — an UNCONFIRMED heuristic instead; rule 6's ON_REGISTRATION final demand needs 20, not wired; the >3-day escalation is a fact on the response only, no real Escalation row (12's sweep is config-rule-driven, not a per-call trigger); portal (26) not built, CRM-on-behalf only.
- Full suite: 91 files / 574 tests, `tsc --noEmit` clean.
- Next: re-survey the remaining R4–R7 knot (16, 20, 26, 27, 29, 30) — 16's only unbuilt dependency was 23, so it may now be fully unblocked; will verify against its actual spec text before picking.

## 04:45 IST — 16 handover gates backend merged (PR #44)

- Confirmed 16 fully unblocked once 23 landed (its only unbuilt dependency); built next.
- `0039_handover_gates.sql` ALTERs the pre-existing `handover_record` (0000_init.sql, legacy `qa.ts::completeHandover`'s own simple flow) with the full lifecycle, and adds 5 new tables (`handover_gate_config`, `handover_gate_run`, `handover_override`, `handover_appointment`, `handover_checklist`) rather than the Data section's separate `handover_case` — same two-producer coexistence pattern as 23's `registration_case`.
- Real spec-vs-code conflict resolved: the pre-existing `handover.ts` pure engine classified Commitments as SOFT, its own comment citing the legacy `docs/spec/` draft as justification and calling the opposite "an unverified premise." This spec's own p17 §9 table (the authoritative build contract) lists it HARD — corrected `DEFAULT_GATE_CLASS` and the two pre-existing tests that had asserted the old behaviour. Kept nine gates (not the config table's eight) since Purpose's own text splits Quality into "hard for critical, soft for minor," which one config row can't carry — `quality` (hard) and an always-soft `snags` gate stay separate.
- All rules built in `handover/{store,gates,core,policy}.ts`: config-driven gate evaluation with per-project override via `handover_gate_config` (fallback to the p17 §9 table); per-gate override (role + reason + evidence/approval as configured; PHYSICAL never overridable); appointment propose/confirm/reschedule (confirm flips the CUSTOMER gate and the case to SCHEDULED, reschedule recreates the customer action); digital checklist seeded to the Data row's own 5-group/26-item skeleton; completion (all hard gates PASSED/OVERRIDDEN, keys `all_handed_over`, both signatures — sets `booking.status`/`unit.sale_status = handed_over`, reuses the pre-existing `warranty.ts::onHandoverCompleted` DLP-window open); close (COMPLETED + a DLP window exist; 30's own onboarding items are unbuilt, flagged not faked); Policy Studio gate-config CRUD.
- Advisor caught a real blocker before landing: `loadOrCreateCase` lazily creates the `handover_record` row on first read of the new module, so the legacy `completeHandover` INSERT (no `ON CONFLICT`) would throw a unique-violation the first time anyone opened the Handover view before the legacy flow ran — fixed with `ON CONFLICT (booking_id) DO UPDATE`, verified with a regression test exercising the exact interleaving.
- Advisor also caught: the checklist skeleton needed seeding (each item `{done, by, at, file_ids}`, not a bare boolean — a naive seed would have made `keys.all_handed_over` always-truthy since a non-null object is truthy); `handover_gate_run` writing 8 rows on every read instead of only on change; `overrideGate` re-evaluating (and writing gate-run rows) before its own role check; and `handover.gate_overridden`/`handover.closed` marked customer-visible when they're internal governance/audit events. All fixed.
- Flagged, not faked: rule 3's forecast (only 23's `forecast_date` is a real term; 06/19 have none); rule 6's close gate (30's onboarding items are unbuilt and explicitly out of scope for this spec); `overrideGate`'s `requires_approval` path doesn't verify the approver against 25's approval-authority matrix.
- Full suite: 92 files / 590 tests, `tsc --noEmit` clean.
- Next: re-survey the remaining R4–R7/R6 backlog (20, 26, 27, 29, 30) for what's fully unblocked now.

## 05:29 IST — 20 cash forecast backend merged

- Confirmed unblocked: its dependency line (19, 21, 23, 06, 04) is now fully built.
- Advisor-scoped before writing code: corrected the initial REGISTRATION_FINAL_DEMAND plan ("last unpaid demand by sequence," which would have mislabeled a POSSESSION demand as registration money); confirmed via grep that `stamp_duty_inr`/`registration_fee_inr` only exist post-execution, so it's modeled as an immediately-REALISED historical line, not a forward forecast. APPROVED_RESCHEDULE deliberately not implemented — `timeline_plan_revision` has no amount/demand_id link to hang a forecast line on.
- `0040_forecast.sql`: `forecast_line`, `forecast_scenario`, `forecast_assumption`, `forecast_snapshot`, `probability_rule`, `period_calendar`, `cash_target`. `forecast/{probability,derive,waterfall,core}.ts` (all pure except `core.ts`'s orchestration), `seed/probability-rules.ts`, `routes-forecast.ts`.
- One winning source type per demand computed pure (`resolveDemandLine`), diffed against the currently-ACTIVE line per demand in `deriveProjectLines` (no-op/REALISED/LAPSED/SUPERSEDED). Scenario lane computed at read time only, never persisted — proven BASE-byte-identical before/after a scenario is created and given assumptions. Waterfall's target/shortfall fail OPEN (null, not 0) when no cash target is set — advisor-mandated, the one place in this codebase "fail closed" doesn't apply.
- **Advisor-caught performance issue, fixed pre-merge:** `deriveProjectLines` (scans every open demand in a project) was running inside `buildForecastView`, which `compareForecast`/`portfolioCompare` also call — an N-project portfolio compare would have re-derived every demand N times. Fixed by hoisting the derive call up into the two real entrypoints (`getForecast`, `takeSnapshot`); reads now use whatever's current.
- Flakiness check (advisor-required before the Build note could claim anything): `registration.test.ts` times out 4-5 tests under a full-suite run — confirmed via `git stash -u` that this reproduces identically on pure pre-spec-20 `main`, so it's genuinely pre-existing, not this spec's regression.
- 32 new tests (pure units + a 200-generated-facts double-counting property test + a real-DB lifecycle/scenario/snapshot integration block), tsc clean, 92/93 API test files, 617/622 tests.
- Scoped the client's "how much time is left" question into TODO.md §0a: ≈3–5 hours to backend-complete on the remaining 6 specs (measured cadence); the full UI+deployed stack is a separate, unmeasured ~15–25 hour estimate, dominated by the ~16-spec UI backlog, not backend logic.
- Next: 26 (customer portal) — advisor/TODO-flagged as the right next pick among the circularly-blocked 26/27/29/30 group, since it has the most downstream consumers.

## 06:20 IST — 26 customer portal backend merged (autonomous run, no check-ins)

- Amarsh logged off at 05:36 IST with explicit instructions to build everything in the specs without stopping or asking questions. Continuing solo from here per that instruction.
- Built additively as new `src/portal/**` rather than "replace `transparency.ts`/`customer.ts`" per the Files line — 6 other files depend on them, measured before deciding not to touch either.
- This codebase had already anticipated 26 heavily: 8 dedicated `customer_*` permission_matrix modules, a `customerCtx()` test helper, and 2 existing `ctx.actor.kind === "CUSTOMER"` branches (change-requests, qa/snags) all pre-dated this build. Reused directly: `t2Payments`, `t4Passport`, `bookingForCustomerUser`, `currentItems` (wrapped, see below), `raiseChangeRequest`/`acceptQuotation`, `createNotification`/`createAction`.
- Widened `registration/core.ts::confirmAvailability` and `handover/core.ts::confirmAppointment`/`rescheduleAppointment` to accept a CUSTOMER ctx on their own booking — both specs' own prior Build notes had named 26 as the unlock for their CRM-on-behalf-only fallback.
- Real bug caught before merge: `uploadCustomerDocument` was first written against a non-existent `customer_document.file_id` column and an unused `RECEIVED` status — fixed by delegating to 22's real `uploadDocument` lifecycle instead of inventing a parallel one.
- Advisor review caught one over-widening: an earlier draft opened `CUSTOMER_MODULES.customer_documents` to WRITE in the shared seeded permission matrix to satisfy a check this build wrote itself — reverted once the ownership check became the real gate, per the module's own documented "own-booking check, not the matrix" pattern.
- New deadlock variant found and fixed on the single-connection PGlite adapter: a bare `db.query()` from a function called *inside* an open transaction hangs, same as the already-known nested-`withTx` case but with no second transaction involved (`bookingHeader` inside `submitCheckIn`'s tx).
- Confirmed (again, via `git stash`) that `registration.test.ts`'s intermittent full-suite timeouts are pre-existing worker-pool contention, not this spec's regression — same flake already logged at 20's landing.
- Full detail in `docs/specs/26-customer-portal.md`'s own Build note and TODO.md §9. Full suite: 94 files / 638 tests, `tsc --noEmit` clean.
- Next: land this PR, then proceed to 27 (management control tower), 29 (communications, now unblocked), 30 (post-handover, now unblocked), 31 (intelligence) — continuing autonomously per standing instruction.

## 11:36 IST — 27 management control tower backend merged (autonomous run, continued from 06:20)

- Replaces `tower.ts`/`tower-view.ts` (PR #8's base) with `management/{scoring,interventions,kpis,exceptions,profitability,portfolio,teams}.ts` and `kpis/{math,queries}.ts` (~29 KPI formulas across 9 domains, seeded UNCONFIRMED). Kept the old `/tower`/`/interventions/:id/act` URLs pointed at the new code (verified live in `apps/workspace/src/api-lifecycle.ts`) rather than renaming.
- Advisor caught two real bugs pre-merge, both fixed with regression tests: (1) the dismiss cooldown didn't cool down anything — a dismissed `(project, category)` slot carried its status forward onto a genuinely different candidate while its own cooldown key (`source_refs`) got clobbered by the recompute; (2) `intervention.computed`/`kpi.snapshot_taken`/`economic_event.recorded` all appended to the event log unconditionally on every GET — same class advisor caught pre-merge on spec 16's `handover_gate_run`. All three now diff before/after and only append on a real change.
- Two KPI formula bugs found by inspection while writing coverage (not by the `it.each` shape test itself, which only proves each formula returns a well-formed result, not a correct one — said so plainly in the Build note): `c_efficiency_pct` filtered on a nonexistent `'partial'` status (real value `part_paid`), undercounting billed amounts; `h_on_time_pct` LEFT JOINed appointments, so every legacy handover completion (no appointment row) read as late.
- Full detail in `docs/specs/27-management-control-tower.md`'s own Build note and TODO.md §9. Full suite: 95/96 files, 694/698 tests (4 pre-existing `registration.test.ts` timeouts, confirmed unrelated), `tsc --noEmit` clean.
- Next: 28 (360 views), 29 (communications, now unblocked), 30 (post-handover, now unblocked), 31 (intelligence) — continuing autonomously per standing instruction.

## 12:10 IST — 28 Customer/Unit/Booking 360 views + Project 360 header backend merged (autonomous run, continued from 11:36)

- Pure composition layer over ~15 existing modules — a background research agent surveyed exact function signatures/return shapes for every dependency first, then this build wrote new lightweight queries only for real gaps (no booking-scoped `listActions` filter, no unit-count/product-mix aggregate, no multi-booking commitments/change-request listing, per-entity Activity feeds off `event`'s own direct `unit_id`/`booking_id`/`customer_id` columns). Tab-manifest architecture: each 360 endpoint returns Overview + a `tabs` manifest naming each tab's own already-built route; tab content stays owned by its owning spec, degrading to a named "not yet available" placeholder where that spec (29) isn't built.
- Advisor caught two real bugs pre-merge, both fixed with regression tests: (1) a permission-matrix bypass — `getCustomer360`/`getCustomerDocuments` were gated `requireRole(STAFF_ROLES)` (matching `getUnit`'s own precedent), but `customer_overview`/`customer_documents` are core `permission_matrix` modules where SITE/FM sit below full READ — `STAFF_ROLES` let them through anyway, handing customer PII to roles the matrix explicitly locks out (same failure family as 26's `customer_documents` READ→WRITE widening, this time a bypass instead of an over-grant); fixed to `authorize(ctx, module, "READ")`, which turned out to match a pre-existing `getCustomer()` gate in `model/customers.ts` this build had briefly reused then deleted as apparent dead code. (2) a write-on-read bug — the Project 360 header's `unit_readiness_avg` called `computeUnitReadiness` once per unit, and that function unconditionally persists a `score_snapshot` row per call; since this header loads on every module screen (rule 4), that's N unwanted writes per load on the hottest path in the app, the same class advisor already caught twice at specs 16 and 27 — fixed by switching to the non-persisting `explainUnitReadiness`.
- Smaller fixes from the same advisor pass: `next_actions`' `ORDER BY priority DESC` sorted the text column alphabetically (CRITICAL last, not first) — fixed with a CASE ordinal; three bare `throw new Error("not_found")` wouldn't map to HTTP 404 through `failHttp` — fixed to `AppError("not_found", "not_found")`, matching existing convention.
- Full detail in `docs/specs/28-360-views.md`'s own Build note and TODO.md §9. Full suite: 96/97 files, 706/711 tests (5 pre-existing `registration.test.ts` timeouts, confirmed unrelated — same file passes 6/6 in isolation), `tsc --noEmit` clean.
- Next: 29 (communications), 30 (post-handover), 31 (intelligence) — continuing autonomously per standing instruction.

## 12:55 IST — 29 communications backend merged (autonomous run, continued from 12:10)

- Reused three prior specs' machinery directly rather than building parallel mechanisms: 26's `customer_update` table/feed for publish-to-portal, 22's merge-field resolution (`buildSourceContext`/`resolvePath`/`formatMergeValue`) for template rendering, and 12's SLA-clock/escalation-ladder chain for rule 6's 48h follow-up escalation. `customer_query_48h` is the first of 12's 13 seeded escalation rules actually flipped `wired: true` — its `sla_policy` row sets `escalation_ladder_id` directly at seed time, the same gotcha already solved once for `snag_sla_policy` (15).
- Advisor caught a real ordering bug pre-merge: `sendCommunicationEmail` sent the email before inserting the `communication` row, so a validation failure after the send would leave a real email sent with no logged row (violating rule 1, "every customer touch is logged"). Fixed by moving the insert + event inside the transaction first, mail last.
- Purpose-conditional dual template approver built per rule 3 (LEGAL for PAYMENT_REMINDER/DELAY_NOTICE, CRM-lead otherwise) — the spec's own prose also names "cancellation" as legal-bearing but the schema's `purpose` enum has no such value, flagged in code rather than invented. Rule 4's frequency guardrail only fires when a template is used (a free-text send with the same copy bypasses the cap) — a real, logged gap, not fixed this pass. The `wired: true` flip on `customer_query_48h` only reaches a fresh DB (`seedEscalationConfig`'s backfill only runs once, at first boot) — same migration-vs-seed-order shape already documented for spec 08.
- Landing 29 turned spec 28's Customer 360/Booking 360 "Communications" tab from a placeholder into a real endpoint, per 28's own manifest design naming 29 as the unlock — updated both view files and their tests.
- Full detail in `docs/specs/29-communications.md`'s own Build note and TODO.md §9. Full suite: 94/95 files, 676/682 tests (6 pre-existing `registration.test.ts` timeouts, confirmed unrelated — same file passes 6/6 in isolation), `tsc --noEmit` clean.
- Next: 30 (post-handover), 31 (intelligence) — continuing autonomously per standing instruction.

## 13:25 IST — 30 post-handover backend merged (autonomous run, continued from 12:55)

- ALTERed the pre-existing `warranty.ts`/`warranty_case`/`home_passport_item`/`service_history` in place, no parallel table set. Reused 26's `customer_check_in`/`sendCheckIn`/`submitCheckIn` for all of rules 1/5/7's DAY_7/30/90 and DLP_CLOSE check-in scheduling, leaving the older pre-26 `checkin_record` mechanism untouched (same "different producer, keep both" precedent 26's own migration established). Extracted a shared `post-handover/dlp.ts` to break a circular import between `core.ts` and `warranty.ts`, resolving DLP policy via 3 sequential fallback SELECTs rather than a `UNION ALL`+`LIMIT 1` (row order under `LIMIT` with no `ORDER BY` isn't guaranteed).
- Advisor caught two real bugs pre-merge: (1) `sweepDlpClosure` only considered `status = 'IN_DLP'`, so a case where FM never ticks one move-in task (e.g. `association_membership` on a project with no association) stayed `ONBOARDING` forever and could never reach `DLP_CLOSED` even after its DLP window fully expired — the onboarding checklist and the DLP clock are independent per the spec's own wording; fixed to `status IN ('ONBOARDING', 'IN_DLP')`, and replaced an inline `UNION ALL`+`LIMIT 1` policy lookup in the same function with a call to the shared `resolveDlpPolicy`. (2) `acceptQuote`'s docstring claimed CRM could record acceptance on a customer's behalf, but the staff branch's own gate (`authorize(ctx, "handovers", "WRITE")`) and the seeded matrix (`"R N R N N N N W W"`) give CRM READ-only on `handovers` — code was already correct, comment was fixed to state the real SITE/FM-only restriction.
- Rule 3's snag-evidence linkage (`warranty_case.snag_id` via 15's `insertSnag`) deliberately left unwired after checking `snag`'s real shape — no `room` field, incompatible fixed `category` enum — flagged as a real vocabulary-mismatch gap, same class as 15/18's own prior calls. `respondAdvocacy` inserts directly into `prospect` via raw SQL rather than calling `sales/prospects.ts::createProspect`, since that function's role gate excludes CRM (the actual caller) and its own transaction would deadlock nested inside advocacy's. `sla_policy.applies_to` extended a third time (`+'WARRANTY_SEVERITY'`); DLP window months and warranty SLA days both seeded UNCONFIRMED.
- Full detail in `docs/specs/30-post-handover.md`'s own Build note and TODO.md §9. Full suite: 99/99 files, 723/738 tests (5 pre-existing worker-pool-contention failures across `collections-sweep`/`authz/mask`/`documents`/`registration.test.ts`, confirmed unrelated — all 4 files pass 26/26 together in isolation), `tsc --noEmit` clean.
- Next: 31 (intelligence, the last unbuilt backend spec) — continuing autonomously per standing instruction.

## 14:05 IST — 31 intelligence backend merged — all backend specs complete
- Customer Health (rules over check-ins/escalations/overdue/commitments/comms/pending-action-age/sentiment), Financial Health (true-risk share/forecast variance/loan gap/waiver leakage/clearance), Journey/Collection/Commitment risk, Next Best Action, and 6 LLM-task suggestion kinds (commitment detection, summary, sentiment, document field-extraction, document inconsistency, snag root-cause) behind the fake adapter in test.
- Continued on top of substantial in-progress work already found in the working tree (a research fork had gone beyond its research-only brief and written most of the scorers) — reviewed it in full against schema/spec before continuing, judged it sound, built the rest on top.
- Advisor caught two real bugs pre-merge: (1) `withinBudget()` read `LLM_MONTHLY_BUDGET_INR=0` as "unset" (`Number("0")` is falsy) rather than a real zero cap — fixed to distinguish unset from any parsed number including 0. (2) `computeCollectionRisk` called the persisting `computeCustomerHealth` instead of `explainCustomerHealth`, so reading a demand's risk silently wrote a `CUSTOMER_HEALTH` snapshot for a different subject as a side effect — same write-on-read class already caught at 16/27/28.
- Corrects a factual error in spec 30's own landed comment (PR #50): checked `qa/snags.ts`/`migrations/0032_qa.sql` and found `snag.room`/`category`/`root_cause` all exist as real columns, and `warranty_case.category` has no CHECK constraint — 30's stated reason for leaving `warranty_case.snag_id` unwired was wrong on both counts. Did not reopen #50 (surgical scope); 31's own `snag-root-cause.ts` correctly writes `snag.root_cause` on accept instead.
- Self-caught before running any test: `journey-risk.ts` was first written against a nonexistent `GateState === "BLOCKED"` value, fixed to the real `stage_instance.status = 'BLOCKED'` signal. Also fixed a real documentation gap in `25-policy-studio.md`'s own master Tabs line (never updated past spec 30).
- Full detail in `docs/specs/31-intelligence.md`'s own Build note and TODO.md §9. Full suite: 100/100 files, 752/752 tests, `tsc --noEmit` clean.
- **All backend specs (01–31) are now backend-complete.** Remaining work is the UI/dashboard backlog (TODO.md §0a estimates 15–25 hours).

## 14:30 IST — e2e isolation prep (PR #52), then 15:00 IST — 25 Policy Studio UI shell (first UI slice)
- Before starting UI work: re-measured the long-carried e2e flakiness (two consecutive full-suite runs, different failure sets both times). Set `playwright.config.ts` to `workers: 1` (matches CLAUDE.md's own already-documented, never-wired CI plan) and added `services/api`'s `npm run db:reset` for a clean reseed. Full per-test DB isolation still open.
- Picked Policy Studio as the first UI slice by fan-out (32 `built:true` tabs registered, the most of any spec). Built `apps/workspace/src/pages/studio/{Shell,GenericTableEditor,RowEditor,HistoryDrawer,api,registry}.tsx`: left nav grouped by owning spec from the real `GET /studio/tabs`, and a working add/edit/draft+publish/history flow for the 10 tables the backend's generic `/studio/:table` envelope covers.
- Screenshotted and functionally exercised at 1440/768/375 against the live dev API (not just typechecked) — this caught two real bugs: an empty optional field submitted `""` instead of `null` and Postgres rejected it for a typed column (`risk_rule.effective_to`), fixed; and a 768px-specific layout squeeze where Studio's own nav+content split competed with the app's sidebar for a 3rd column, fixed by moving the split to the `lg` breakpoint.
- Incidentally re-validated PR #52 the same session: force-killing a stale background API process mid-testing corrupted its PGlite lock file exactly as TODO.md's pre-existing note describes — `npm run db:reset` fixed it on the first try.
- The other ~22 registered tabs show an honest "not built here" / "has its own screen elsewhere" state — no bespoke tab UI was attempted this slice.
- Full detail in `docs/specs/25-policy-studio.md`'s own Build note and TODO.md §9. Workspace suite: 6/6 files, 67/67 tests; `tsc --noEmit` clean; full e2e: 72/73 (1 pre-existing, unrelated failure).
- Next: continue the UI backlog slice by slice (workspace pages for the backend-complete specs, then the customer-facing `my-pranava-home` portal).

## 15:40 IST — advisor review on Policy Studio, then 11 My Day UI (second UI slice)
- Advisor review of the just-landed Policy Studio UI (before declaring it done) caught two real issues, both fixed pre-merge: a second `<h1>` (Shell's own nav title alongside `GenericTableEditor`'s `PageHeader`) — fixed to `h2` with a new one-`h1` test; and `fromFieldValue`'s blank-field mapping only handled plain columns, not array/json ones — several generic tables carry a nullable `product_types text[]` where NULL means "every product type," so editing any unrelated field on an unset row was silently narrowing that to "none" with no error. Fixed universally; also corrected the accompanying code comment, which had claimed "every table here has these as nullable columns" without checking the DDL (several plain columns are in fact NOT NULL).
- Picked 11 (My Day) as the second UI slice via advisor: zero forward dependencies (only 10/06/01, all built, no stubs needed) and named second in the spec's own final-state paragraph ("An employee opens My Day and knows what matters").
- Built the five ranked sections as Tabs, Team view for heads, real server-generated `why_now` rendered verbatim (no client-side duplication of that logic).
- **Real regression caught by the e2e suite before merge**: adding "My Day" as the first NAV entry silently changed SUPER_ADMIN's default landing page (it had no explicit `ROLE_HOME`, falling through to `visible[0]` — previously "site" only by array-order accident). Fixed with an explicit `ROLE_HOME.SUPER_ADMIN = "site"` — cleanly, twice-verified (4 landing-page tests failed pre-fix, 0 post-fix).
- Environment finding, not a My Day defect: the shared local PGlite dev DB corrupted twice during this slice's own functional testing ("unexpected data beyond EOF in block 0" — a more severe cousin of the already-documented stale-lock-file symptom). `npm run db:reset` (PR #52) plus a clean restart recovered it both times. **Honest scope note**: a full clean-DB suite pass was not independently obtained this slice — the one freshly-reset-DB run corrupted mid-run; the 70/73 figure ran against a DB already carrying prior-run state, so its "pre-existing" classification is inherited from TODO.md's own earlier log, not reconfirmed clean here.
- Advisor review before declaring this slice done (same discipline as Policy Studio) caught two more real issues, both fixed pre-merge: rule 6's empty state showed `done_today` (closed actions) in the slot the spec's own example implies is a forward-looking open-item count — no such count exists server-side, so fixed to "Nothing due right now." rather than fill the slot with an unrelated number; and `TeamView`'s `getTeamDay(projectId)` call was unguarded against `projectId === ""` (true for SUPER_ADMIN until the project switcher loads) — guarded with the same pattern `SiteProgress.tsx` already uses. Also added `nav.test.ts`, pinning the `defaultViewFor` fragility that caused the regression above.
- Full detail in `docs/specs/11-my-day-ranking.md`'s own Build note and TODO.md §9. Workspace suite: 9/9 files (`nav.test.ts` added), 75/75 tests; `tsc --noEmit` clean.
- Next: continue the UI backlog slice by slice.

## 16:50 IST — 10 Action Drawer (third UI slice)
- Filled a real, pre-existing gap in a spec previously marked backend-complete: `GET /actions/:id` was named in 10's own API list but never built. Added `getAction()` and its route, then built `apps/workspace/src/components/ActionDrawer/{api,TransitionActions,ActionDrawer}.tsx` and wired it into My Day (clicking a row now opens the drawer).
- Transition buttons are purely structural (status + family) — no client-side role/ownership simulation; the server's 403 surfaces as an inline error, same "let the server say no" precedent as Policy Studio.
- **The most significant finding of this slice, and a self-inflicted near-miss**: this same spec's own prior Build note (backend landing) had already flagged that closing/approving/cancelling a task-backed action directly (bypassing `completeTaskInstance`) desyncs `task_instance`/the journey, and said this "must be fixed... before that UI ships." Shipping this drawer wired into My Day *is* that UI — and `myday/core.ts`'s `ACTION_QUERY` has no filter excluding task-backed actions, so the path was genuinely live, not hypothetical. Caught by advisor review before landing, not by any test. Fixed with the minimal correct scope (not the full event-subscriber rewrite rule 7 still owes): `getAction` now returns `task_instance_id`; the drawer hides Close/Approve/Cancel for a task-backed action and shows a plain-text note instead; the server refuses those three from any external caller too. Three new regression tests in `journey/instances.test.ts` prove both the refusal and that `completeTaskInstance`'s own internal call still works.
- Advisor also caught a real double-fire bug: the button group re-enabled against a stale `action` prop in the window between a transition's POST resolving and the parent's refetch landing, letting a fast double-click re-send the same transition into the server's own conflict guard. Fixed with a `settling` state cleared only when a fresh `action` object actually arrives, regression-tested (fire Start, click twice more, assert exactly one POST).
- A failing test caught a real UX design flaw: the initial design routed every transition, including ones needing no input, through a two-click confirm panel. Split into immediate-fire (Claim/Start/Unblock/Submit-for-approval) vs. confirm-panel (reason-required or note-optional) transitions.
- Advisor caught a silent-failure gap: the checklist checkbox and evidence Verify/Reject buttons had a bare `.then(onChanged)` with no `.catch`, even though the underlying calls can genuinely 403 — fixed with a shared inline-error helper.
- Folded in a fix flagged (not yet applied) in 11's own Build note: `nav.test.ts`'s second test used the full unfiltered NAV list as "visible," which every role can trivially see, so it could never fail. Replaced with a real test checking each `ROLE_HOME` entry against that role's own filtered visible set, plus a fallthrough case.
- Real scope cuts, flagged in the drawer's own header comment: no status stepper (the transitions History section already shows the real, branching path — judged redundant to duplicate), owner/backup render as raw ids, "why it exists" is plain text not a link, no evidence-upload UI.
- Full detail in `docs/specs/10-universal-action.md`'s own Build note and TODO.md §9. Backend targeted run (`actions/core.test.ts` + `journey/instances.test.ts`): 37/37. Workspace suite: 10/10 files, 85/85 tests; `tsc --noEmit` clean on both apps.
- Advisor's final pre-landing pass caught one more real thing: `cancelAction`'s unconditional task-backed guard was worded as simply correct rather than conservative — corrected the comment to say a future journey-level cancel path (closeJourney, a cancelled booking) would need the same `!maybeTx` discriminator close/approve already use, since no such path exists yet to actually cancel a task-backed action.
- Added `apps/workspace/e2e/myday.spec.ts` (4 tests) and screenshotted My Day at 1440/768/375. Superadmin's seed My Day is empty, so verifying the task-backed note's actual placement (advisor's specific ask) needed real data — booked and accepted a fresh villa live, then opened CRM's resulting task-backed action in a real browser. That surfaced a real bug, but an environment one: `GET /api/actions/:id` 404'd against an id `GET /me/day` had just listed, traced to the API dev server's `tsx watch` process never restarting after this slice's own file edits (OneDrive-synced working directory is the suspected cause) — over an hour of stale code silently serving live requests. Restarting it fixed it with no code change; worth remembering as a general gotcha for this environment. Confirmed after restart: the note sits directly above the action buttons at all 3 breakpoints, Claim/Start stay available on a task-backed action, only Close/Approve/Cancel are hidden.
- Landed via the standard branch/PR/merge/mirror workflow (PR #55).

## 18:04 IST — 10 Departmental Queues (fourth UI slice, closes out spec 10's Screens/Files list)
- Built `apps/workspace/src/pages/Queues.tsx`: role tabs for all 10 departments, claim (unassigned rows, any staff), bulk reassign (Management/SUPER_ADMIN, client-gated), status-count badges, shared `ActionDrawer` on row click.
- Investigated rather than built the other remaining Files-list item: `studio/ActionTypes.tsx` was never created because spec 25's generic Policy Studio table editor already covers it end to end (`GENERIC_TABLES.action_type` + `TAB_TO_TABLE["10.action_types"]`, backend `TAB_REGISTRY` entry already `built: true`) — live-verified Studio → Action types renders all 7 seeded rows with full Add/Edit/History. Zero new code needed; recorded as a permanent Files-list divergence, not a gap.
- **Real bug caught by advisor review before landing**: the status-count badges were sourced from `GET /api/queues/:role` (`departmental_queue`, no `project_id`) while the row list below came from project-scoped `listActions` — the two could disagree the instant an actor has more than one project. Reproduced live: switching the project selector on the Sales tab correctly emptied both the badge and the list only after the fix (badge would have stuck at "New: 3" over an empty list before it). Fixed by deriving counts client-side from the same rows; logged `departmental_queue` needing a `project_id` before it gains a UI consumer as a precondition for whoever builds on it next.
- **Real bug caught by Playwright, not flakiness**: switching queue tabs briefly flashed the previous tab's rows (the state-clearing effect committed a render after the tab switch, not in the same one) — Playwright's fast tab-cycling in the interaction test surfaced it as a `row.click()` timeout on a row that belonged to the tab just left. Fixed by clearing rows inside `onValueChange` itself; confirmed stable across 3 repeated e2e runs after.
- **Real bug in the e2e spec itself, same class `myday.spec.ts` had already named once**: `queues.spec.ts` was waiting for the tabpanel container to be visible (true instantly, mid-fetch) rather than for the fetch to settle, producing genuinely blank screenshots at 768/375px, reproduced 100% of the time. Fixed with a shared `waitForSettled()` helper; all 3 breakpoint screenshots re-reviewed after the fix and confirmed correct.
- Full detail in `docs/specs/10-universal-action.md`'s 2026-09-06 Departmental Queues Build note and TODO.md §9. Workspace suite: 11/11 files, 92/92 tests; `tsc --noEmit` clean; targeted Playwright (`queues.spec.ts`, 4 tests) stable across 3 repeated runs.
- Landed via the standard branch/PR/merge/mirror workflow (PR #56).

## 18:40 IST — 05 Journey Template Studio (fifth UI slice, a genuine gap masked by a false registry flag)
- Discovered, not assigned: `studio/registry.ts` had claimed `built: true` for `05.journey_template_studio` with zero frontend implementation behind it, and `Shell.tsx`'s fallback text actively lied about why ("has its own dedicated screen elsewhere in the app"). Found by grepping the codebase for real usages and finding none outside a roadmap tracking file.
- Backend was already complete from an earlier slice (versioning, publish, DSL). Added the 2 small pieces the Screens line needed: `listVersions(templateId)` and a `GET /journey-template-versions/:id/preview` route wrapping the existing pure `previewVersion`.
- Built the full screen: template/version pickers, stream-grouped stage/task swimlane cards, stage/task edit drawers with execution-type-conditional fields, a dependency list + inline editor, a publish dialog with a client-computed diff vs. the prior PUBLISHED version (no backend diff endpoint exists) and migration-rule selection, and a preview dialog. Wired in via a new `BESPOKE_TABS` map in `Shell.tsx` for tabs with a bespoke screen instead of the generic table envelope.
- Scope cuts, flagged not faked: dependency "lines" render as a plain textual list, not SVG connectors (the swimlane grid has no fixed pixel geometry to anchor a line to); no "Assign to project" action (confirmed via TODO.md's own breakdown that this belongs to spec 06's not-yet-built Project Journey Control screen).
- **Two real bugs found only by live-clicking through the app, neither caught by RTL mocks nor Playwright's mocked assertions**: (1) "New draft from this version" left the old still-PUBLISHED version selected instead of switching to the new draft — the parent's "keep current selection" default preferred the old id; fixed by passing the new version's id explicitly. (2) After Publish, the panel kept showing DRAFT + edit controls despite the dropdown correctly flipping to PUBLISHED — `key={versionId}` doesn't remount on a status-only change; fixed with an explicit reload in the `onPublished` callback.
- Also corrected while touching `Shell.tsx`: the same false "has its own dedicated screen elsewhere" claim would still fire for two other genuinely `built: true`-but-no-UI tabs (`24.hold_policy`, `25.config_export_import`) — reworded the fallback to state plainly that the backend is built but the Studio UI isn't, and logged both as open gaps in TODO.md §9 rather than leaving a known-false string live.
- Full detail in `docs/specs/05-journey-templates.md`'s 2026-09-06 Build note and TODO.md §9. `services/api`: 14/14 unit tests, `tsc --noEmit` clean. `apps/workspace`: 6/6 new RTL tests, `tsc --noEmit` clean, targeted Playwright (`journey-template-studio.spec.ts`, 5 tests) green. Screenshots reviewed at all 3 breakpoints (1440/768/375) — desktop professional and clean with real East Crest data across all 6 streams; tablet/mobile stack correctly, the tall page height being a pre-existing `Shell.tsx` nav-stacking choice shared by every Policy Studio tab, not specific to this slice.
- Known side effect: live testing (New draft + Publish) left East Crest's journey template at v3/DRAFT instead of its seeded single-PUBLISHED state — run `db:reset` before the next full-suite checkpoint.
- Landed via the standard branch/PR/merge/mirror workflow (PR #57).

## 19:10 IST — 06 SLA Policy Studio tab (sixth UI slice, resolving a documented but stale "bespoke" design note)
- Two independent comments (`0023_policy.sql`'s header and `studio/core.ts`'s header) both said `sla_policy` needs bespoke draft/publish logic mirroring 05's `journey_template_version`, not the generic envelope. Checked the premise against the schema: `sla_policy.code` is UNIQUE, so a true 05-style multi-concurrent-version model is architecturally impossible without a migration (out of scope, "ask first" territory) — but the generic envelope's actual publish mechanism is a plain in-place `UPDATE`, which `risk_rule`/`probability_rule` already prove works safely for a table with its own `effective_from`/`version` columns. Resolved by keeping the same safe storage, and building the genuinely bespoke piece the codebase had explicitly flagged as still missing: `previewStudioChange` had thrown "not implemented" for every table with a comment naming `sla_policy`'s open-`sla_clock` count as the one real computable case — implemented it (`COUNT(*) FROM sla_clock WHERE policy_id = $1 AND stopped_at IS NULL`) and built a dedicated `SlaPolicyStudio`/`SlaPolicyDrawer` screen (draft → real impact preview → confirm publish) instead of `GenericTableEditor`. Both stale comments corrected to state the actual, current reasoning rather than left disagreeing.
- **Real bug found live (MCP browser, not any automated test)**: the "Applies to" `<Select>` only listed 06's original 3 Data-row values; the real CHECK constraint (widened by 3 later specs' migrations) allows 6, and 7 of the 26 seeded policies use the missing 3 — Radix rendered the Select blank for all 7 with no error. Fixed by adding all 6 real options with spec-referencing labels; confirmed live afterward that `customer_query_response` now displays "Customer query (29)" correctly.
- **Real gap found by advisor review before landing**: the drawer had collapsed two distinct dates into one `effectiveFrom` field — `sla_policy`'s own business `effective_from` column, and the unrelated `policy_version` publish-date stamp that `RowEditor.tsx` already keeps separate for `risk_rule`/`probability_rule` for exactly this reason. A user editing a policy with an old business `effective_from` would have silently either overwritten it with today's publish date or published under a stale date. Split into two fields; added the missing new-policy-flow RTL test that exercises this path end to end (advisor noted only the edit-existing path had test coverage).
- Confirmed, not guessed: `delay_reason` has zero seed rows in this DB (grepped every seed script — none inserts one), so the drawer's pause-reasons fieldset correctly renders nothing; not a bug.
- Full detail in `docs/specs/06-timeline-sla-engine.md`'s 2026-09-06 Build note and TODO.md §9. `services/api`: 18/18 unit tests (3 new sla_policy cases), `tsc --noEmit` clean. `apps/workspace`: 4/4 studio files, 17/17 tests (7 new for this slice), `tsc --noEmit` clean, targeted Playwright (`sla-policies.spec.ts`, 5 tests) green. Screenshots reviewed at all 3 breakpoints — desktop clean and professional with real seeded policies; tablet/mobile scroll horizontally rather than clip (verified via direct `scrollLeft` manipulation, not just visually), matching `GenericTableEditor`'s existing responsive-table convention.
- Landed via the standard branch/PR/merge/mirror workflow (PR #58).

## 19:49 IST — Studio registry-only batch (seventh slice: 13 tables across specs 07/08/12/14/15/17/19/24, zero bespoke UI)
- Forked a survey of every remaining `not built` Policy Studio tab (registry.ts vs. Shell.tsx vs. each owning spec's own Screens section vs. TODO.md's existing deferrals) to produce a concrete triage: (a) already-documented won't-build, (b) a real flat-column table exists → registry-only fit, (c) genuinely needs bespoke UI. Verified the survey's claimed column shapes against the actual migrations directly (not trusted blind) before building on them — found the survey right on every table except needing the full ALTER-history read to get `component_definition`/`change_category`/`overdue_reason`'s complete column lists (each widened by a later migration).
- Registered 13 tables in one slice, same zero-bespoke-code shape as `10.action_types`: `component_definition` (07, backs 2 tabs), `change_category` (08), `escalation_rule`/`escalation_ladder`/`materiality_threshold` (12), `score_weight` (14), `qa_checklist_template`/`snag_sla_policy`/`contractor` (15), `handover_checklist_rule`/`return_reason` (17), `overdue_reason` (19), `sales_policy` (24). Backend `TABLE_REGISTRY` (studio/core.ts) + `TAB_REGISTRY` built-flip (studio/registry.ts) + workspace `GENERIC_TABLES`/`TAB_TO_TABLE` mirror (registry.ts) — no new frontend components.
- Department-owned tables (SITE/QA/CRM/ACCOUNTS) needed `SUPER_ADMIN` added explicitly to `TABLE_REGISTRY`'s `editRoles` alongside the department — `requireRole` has no implicit SUPER_ADMIN bypass the way `tabsForRoles`'s `can_edit` UI flag does; missing this would have 403'd a SUPER_ADMIN who saw "can edit" in the nav.
- **Real, pre-existing `GenericTableEditor` bug found live (MCP browser, not any automated test) and fixed**: switching between two tables with *different* primary-key column names (e.g. `escalation_rule`'s `rule_key` → `sales_policy`'s `id`) produced a real React "duplicate key" console warning — one transitional render frame kept the OLD table's `rows` in state while `def` had already updated to the NEW table, so every row's computed key (`r[def.primaryKey]`) came back `undefined`. Reproduced deterministically 3 times on mismatched-PK transitions, never once on same-PK transitions (Ladders→Materiality→QA templates→Contractors, all `id`) — confirming the mechanism. Not caused by this batch (any two pre-existing tables with different primary keys, e.g. `project_calendar`'s `id` vs `delay_reason`'s `code`, could already trigger it) but only surfaced now because more registered tabs meant more transitions exercised during testing. Fixed with one line — `key={tableName}` on `GenericTableEditor`'s usage in `Shell.tsx`, forcing a full remount (synchronous state reset) on every table switch. Verified live afterward: the exact reproducing sequence (Escalation rules → Filter thresholds) now shows zero console errors.
- Live-clicked through all 13 newly-registered tabs plus 2 pre-existing ones as controls; confirmed real seeded data renders correctly for 11 of the 13 (jsonb columns, arrays, booleans, dates all round-trip), and an honest empty state for the 2 with no seed rows yet (`contractor`, `sales_policy`) rather than a broken table. Edited a real row (`component_definition`'s `structure`, readiness_weight 1→2) end-to-end through draft+publish to confirm the envelope itself works for a newly-registered table, not just its listing — reverted via `db:reset` afterward since this mutated seeded data.
- **Not registered, logged as genuine gaps in TODO.md §9**: `01.field_sensitivity` (composite PK `(module, field)` — the generic envelope is single-column-primaryKey only); `19.payment_plans` (parent + ordered child milestone rows — needs a bespoke screen, not a flat form); `04.unit_types`, `04.applicant_limits`, `12.notification_defaults`, `19.clearance_checklist_threshold` (confirmed no backing config table exists for any of these by reading the schema directly).
- Full detail in TODO.md §9. `services/api`: 22/22 tests (`studio/core.test.ts`, +4 new covering the batch: a cross-table column-sanity check, a department-role-scoping check, a `version`-column table, and a multi-jsonb-column table), `tsc --noEmit` clean. `apps/workspace`: `tsc --noEmit` clean, 18/18 Studio RTL tests (+1 new: a `Shell.test.tsx` regression test for the `key={tableName}` fix, confirmed to fail without the fix and pass with it). `db:reset` run before and after live verification.
- **Advisor pre-landing review**: confirmed by grep that `sales_policy`/`contractor` genuinely have no seed path (not a masked RLS bug); confirmed `exportProjectConfig` is a hand-written 5-table export, not a `TABLE_REGISTRY` iteration, so none of this batch's project-scoped tables leak into project-config export; added the `Shell.test.tsx` regression test above; fixed two stale "10 tables" comments; decided to keep `07.progress_components`/`07.freshness_thresholds` both live on the shared table rather than hiding one (documented in TODO.md). Targeted Playwright regression (`journey-template-studio.spec.ts` + `sla-policies.spec.ts`, 10 tests) green after the `Shell.tsx` comment trim.
- Landed via the standard branch/PR/merge/mirror workflow (PR #59).

## 20:20 IST — 08.gate_expiry_sources investigation + 19.payment_plans bespoke Studio screen (eighth slice)
- `08.gate_expiry_sources` investigated: confirmed a documentation-only duplicate of `08.change_gate_rule_studio`, not new work — spec 08's Data table backs no separate `gate_expiry_source` table; the "source" of a gate's expiry (rule 2's `expected_close_at`) is `change_gate_rule`'s own `trigger_component_code`/`trigger_event`/`closing_lead_days` columns, which that (still-unbuilt) Studio screen will edit once built. Stays `built: false`; documented in `services/api/src/studio/registry.ts` and spec 08's Build note. Docs-only, committed directly to main (same precedent as the earlier PR-number backfill).
- `19.payment_plans` built as the genuinely bespoke screen the prior slice deferred: `payment_plan` (parent) + ordered `payment_plan_milestone` (child) rows can't fit the generic envelope's flat-column form. New backend module `payment-plans.ts` — plain CRUD, not draft/publish (neither table carries a version/effective_from column) — with routes in `routes-collections.ts` (`GET /api/payment-plans`, `GET/PUT /api/payment-plans/:id`, `POST /api/payment-plans`). A save replaces the whole milestone list in one transaction rather than reconciling add/remove/reorder separately; confirmed safe by reading `demand`'s real schema directly — it copies `milestone_key`/`label`/`trigger_event`/`sequence` at generation time, no live FK to `payment_plan_milestone.id`, so an edit can never orphan an already-generated demand. Write roles `["ACCOUNTS", "SUPER_ADMIN"]` — SUPER_ADMIN listed explicitly, the same `requireRole`-has-no-implicit-bypass gotcha the prior slice found.
- Frontend: `PaymentPlanStudio.tsx` (one card per plan, milestone table ordered by sequence) + `PaymentPlanDrawer.tsx` (inline milestone editor: add/remove rows, a live running % total that turns red and names the mismatch when it doesn't sum to 100 — without hard-blocking save, since neither the PDF nor the backend requires exactly 100%). Wired into `Shell.tsx`'s `BESPOKE_TABS`.
- **Environment note**: the API dev server runs via `tsx src/server.ts` (no watch), so the backend code changes (registry flip, new routes) didn't take effect until the server was restarted — found when the Studio tab still showed "Payment plans not built" after the frontend had already picked up the new component via Vite HMR. Restarted the API process; re-authenticated afterward (the restart invalidated the browser session).
- Live-verified: both real seeded plans (`plan_eastcrest`, `plan_standard`) render their 5 milestones correctly ordered, all summing to 100%; opened the edit drawer on the real seeded plan and confirmed the pre-fill, the running-total warning (edited one milestone to 50%, saw "Total: 140% — doesn't add up to 100%"), then Cancelled without saving — confirmed via re-snapshot that both plans were unchanged; ran a full New plan → fill → Save round-trip through the real API and confirmed the new plan appeared in the list. `db:reset` afterward since the create wrote a real row; API server restarted clean on the fresh DB.
- `services/api`: 28/28 tests (`payment-plans.test.ts` — 6 new: list/get, role gating, empty/duplicate-key/duplicate-sequence validation, wholesale milestone replacement leaving other plans' rows untouched), `tsc --noEmit` clean. `apps/workspace`: 25/25 Studio tests (`PaymentPlanStudio.test.tsx` — 7 new), `tsc --noEmit` clean. Targeted Playwright regression (`journey-template-studio.spec.ts` + `sla-policies.spec.ts`, 10 tests) green. Screenshots reviewed at 1440/768/375 — clean, matches design language, tables scroll rather than clip on mobile.
- **Advisor pre-landing review**: caught a real bug before it shipped — the drawer hardcoded `project_id: null` on every create, and `updatePaymentPlan` didn't accept the field at all, so every plan an Accounts user created through this screen was unreachable by `demands-schedule.ts`'s per-project fallback lookup (no `OR project_id IS NULL` there); confirmed by reading `projects.ts::cloneStandardPlan`, which is the only real use of a null-`project_id` plan (a template copied onto new projects at creation, not itself selectable for demand generation). Fixed: added a Project selector to the drawer (backed by the existing `GET /api/projects`, with the null option explicitly labelled "seeds new projects; not usable for demand generation") and `project_id` to `updatePaymentPlan`. Also grepped the 3 other files referencing these tables (`forecast/derive.ts`, `registration/core.ts`, `sales/booking.ts`) and confirmed none join on `payment_plan_milestone.id` or switch on `basis`, so the whole-list-replacement design still holds. Two smaller fixes: `validateMilestones` now trims a key before the duplicate check (an untrimmed `" m1"` used to pass as distinct from `"m1"`); each milestone row is now a `fieldset`/`legend` group (`sr-only` legend, "Milestone N") so a screen reader gets a row boundary between otherwise-identical field labels. Added spec 19's Build note a line citing spec 19's own "kind/milestones jsonb/versioning stay future work" as the reason this table deliberately has no draft/history trail. 2 new backend tests, 2 new frontend assertions. Live re-verified: picked "East Crest" from the new selector on a new plan, saved — it shows `p_eastcrest`, not "standard template". `db:reset` run again after.
- Landed via the standard branch/PR/merge/mirror workflow (PR #60).

## 21:40 IST — 06 Journey Timeline, Project Journey Control, Stage/Task detail (ninth slice, spec 06's three remaining Screens)
- Backend: `journey/plan-revision.ts` (`createPlanRevision`/`listPlanRevisions`, `POLICY_STUDIO_ROLES`-gated — a plan revision rewrites schedule the same way rule 8's close-journey restriction does), `journey/task-detail.ts` (`getTaskInstanceDetail` — dates, clock + pause history, dependency refs, evidence `action_id`), `journey/control.ts` (`getProjectJourneyControl` — every open journey with health/current-stage-per-stream/handover/slippage/top-delay-reasons). Four new routes. `getJourneyForBooking` widened to join template labels via `readVersionContent` so the timeline never re-derives names client-side.
- **Real bug found and fixed**: `plan.revised` is spec 06's own literal Events-list name but had never been added to `events/registry.ts::EVENT_TYPES` — `createPlanRevision` became its first real emitter and would have hit a live `event_type_fkey` violation. Fixed; also caught (before landing, not by advisor this time — a self-check re-reading `registry.test.ts`'s coverage mechanism) that a first draft of the test only mentioned the string without asserting the event row existed, satisfying the coverage check vacuously — added a real emitter-assertion test.
- Frontend: `apps/workspace/src/pages/journey/{api,JourneyTimeline,StageStatusChip,StageTaskDetailDrawer,PlanRevisionDialog,ProjectJourneyControl}.tsx`. `PlanRevisionDialog` is one component with two callers (single journey from the timeline's "Revise plan"; multi-journey bulk from Project Journey Control's row-select) — applies via one `createPlanRevision` call per journey, no dedicated bulk endpoint, since each journey's own `timeline_plan_revision` row is the real unit of record either way. Wired `journey-control` into `nav.ts` (appended at the end) scoped to `SITE/QA/CRM/MANAGEMENT/SUPER_ADMIN` (project-wide cross-customer view, narrower than Queues) and `Workspace.tsx`. `Customer360.tsx` got a "View journey" button per booking (needed `booking_id` added to `Customer.bookings[]`, `model/customers.ts`, and `api.ts`'s type).
- **Two real bugs found live (MCP browser), neither caught by any unit/RTL test**: (1) `getJourneyForBooking` legitimately 404s `not_found` for a booking with no journey yet — the frontend's original `.catch(() => setError(true))` showed "Couldn't load this journey" instead of the honest empty state that was already built and unreachable; fixed by checking `e.code === "not_found"`, and corrected the RTL mock which had made the same mistake (`200+null` instead of a real `404`). (2) At 375px, the journey-timeline header actions (Segmented + up to 3 buttons) overflowed the viewport horizontally (`body.scrollWidth` 430 vs `clientWidth` 360) — traced past a local `flex-wrap` fix that had no effect to the real root cause in `@homeflow/ui`'s shared `PageHeader.tsx`: its actions wrapper had `shrink-0` with no `flex-wrap`, sizing to unwrapped max-content width regardless of the children's own ability to wrap. This is a generic defect any page with more than ~2 small action buttons would hit at narrow widths, not specific to this slice — fixed at the shared-component level and re-verified `PaymentPlanStudio`'s single-button header and `SiteProgress` for no regression.
- **Live end-to-end verification against real generated data, not just seed rows**: the dev DB had zero `delay_reason` rows and zero journeys that had actually gone through `sales_handover.accepted` (seeded "active customers" were inserted directly, bypassing the event). Seeded a real `delay_reason` (`TOWER_SLAB_DELAY` — first attempt used category `CONSTRUCTION`, outside the real 6-value `delay_reason_category_check`; corrected to `VENDOR` after the live constraint violation) via the already-built Policy Studio tab. Booked Villa V101 through Sales end-to-end, accepted it via CRM to trigger a real `instantiateJourneyForBooking`. Opened the resulting Journey Timeline (all 6 streams, real stage/task rows, a genuine SLA clock with a real `START` event). Opened a task's detail drawer (dates, clock, dependencies, evidence-link button). Ran a real single-journey plan revision on Construction Progress through the live dropdowns — stage card updated to the new dates with correct variance/slippage. Confirmed Project Journey Control picked up the same journey with the right health, current-stage-per-stream tags, and the revision's delay reason under "Top delay reasons"; selected the row and confirmed the bulk "Revise plan for N journeys" action appears. Screenshots reviewed at 1440/768/375 for all three screens.
- **Also found (unrelated, pre-existing, not fixed here)**: running the full backend suite surfaced `management.test.ts`'s DELAY_COST-pricing test failing (`5 * per_day_rate` expected, `6 *` received) — reproduced identically against a clean `git stash` of this session's changes, confirming it predates this slice and isn't caused by it. Same "TODO §9 UTC bug" family (a day-count off-by-one against `CURRENT_DATE`/`todayIst()`); logged in TODO.md §9, left for whoever picks up that cleanup.
- `services/api`: 92/92 journey+registry tests (14 new: `plan-revision`/`task-detail`/`control`), `tsc --noEmit` clean. `apps/workspace`: 136/136 tests (23 new across the 4 new/touched screens), `tsc --noEmit` clean. Full backend suite re-run for landing: 786/787 (only the pre-existing, unrelated `management.test.ts` DELAY_COST failure above). `db:reset` needed after this session (new booking/customer/journey/delay_reason/revision) — flagged for the landing step.
- **Advisor pre-landing review caught 3 real gaps, all closed**: the `PageHeader.tsx` fix had only been checked against 2 of ~10 real consumers (audited every one — all others pass a plain single `Button`, inert under the fix; confirmed with a real run of `journey-template-studio.spec.ts`/`sla-policies.spec.ts`, 10/10 pass unchanged); confirmed (not assumed) the test suite's PGlite instance is fully isolated from the dev DB `npm start` uses, so `db:reset` was never a test-correctness blocker; added the slice's missing Playwright spec, `e2e/journey.spec.ts` (7 tests, 3-breakpoint screenshots of both new screens + an explicit `body.scrollWidth <= clientWidth` assertion at 375px — the direct regression guard for bug #2 above, which no RTL test can check). Found and fixed two more real bugs writing it: both the CRM customer list and Customer360's own bookings fetch were being read before their async load settled, silently counting 0 — a genuine race condition, not a test artifact.
- Landed via the standard branch/PR/merge/mirror workflow (PR #61).
