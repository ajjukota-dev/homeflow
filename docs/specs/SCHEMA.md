# SCHEMA — ground truth from merged migrations (R0.5)

Generated by reading `services/api/migrations/0000_init.sql` through `0003_canonical.sql`
in full (2026-09-05). This is what the database **actually** looks like on `main` today —
not what any spec assumes. When a spec's `## Data` section is about to become real code,
diff it against this file first. Every drift found so far is logged in **§ Known drift**
below with the fix already applied to spec text (or the decision still open).

This file does not repeat every column of every table verbatim forever — re-generate the
affected section from the migrations whenever a new one lands, rather than trusting this
doc blindly once R1+ starts adding `0004_*.sql` onward.

## Migrations merged so far

| File | Owns |
|---|---|
| `0000_init.sql` | Original pre-R0 schema: project, component_definition, change_category, change_gate_rule, unit, unit_progress, customer, payment_plan(+milestone), collection_policy, overdue_reason, booking, booking_applicant, demand, receipt, promise_to_pay, loan_case, handover_policy, document_template, generated_document, registration_case, qa_evidence, snag, handover_record, dlp_window, home_passport_item, warranty_case, service_history, checkin_record, intervention |
| `0001_identity.sql` | 01-identity-access: user, session, invite, password_reset, role, user_role, team, project_team_assignment, permission_matrix, field_sensitivity, customer_login, auth_event |
| `0001_platform_llm_call.sql` | llm_call (03-platform-deploy) |
| `0002_event.sql` | 02-event-log: event_type, event (append-only, triggers reject UPDATE/DELETE), event_delivery_failure |
| `0003_canonical.sql` | 04-canonical-model: portfolio, project_hierarchy_node, code_sequence; ALTERs project/unit/customer/booking/booking_applicant; `unit_history` view |
| `0004_journey_templates.sql`–`0024_collections.sql` | R1–R3 feature migrations (05, 06, 10, 25, 19) — not individually re-cataloged here yet; see each spec file's own Data section |
| `0025_rls.sql` | P1 of `docs/reports/2026-09-05-branch-review.md`'s consolidation: `homeflow_app` non-superuser role, blanket grants + `ALTER DEFAULT PRIVILEGES`, RLS policies on the `project` table (scoped by `id`) and every table carrying a direct `project_id` column (staff realm only; customer-realm and join-reachable tables are follow-up work — the migration's own header comment has the full audit). Empirically verified against PGlite via `SET ROLE` — see `src/rls.test.ts`. GUCs are not yet wired into any request path (P1b, separate PR). |

## Real enums / vocabularies (as CHECK constraints or code, not spec prose)

- **`unit.sale_status`** (DB, lowercase): `available, held, booked, registered, handed_over, cancelled_release`. Spec vocabulary (`AVAILABLE`…) is a translation layer in `model/status.ts::toSpecUnitSaleStatus` — the DB was never renamed (documented cost: ~10 call sites).
- **`booking.status`** (DB, lowercase): `draft, confirmed, submitted, returned, crm_accepted, active, registered, handed_over, cancelled, transferred`. Same translation layer (`toSpecBookingStatus`/`toDbBookingStatus`), transition graph in `BOOKING_TRANSITIONS`. **`crm_accepted` is a legal DB value and a defined transition target, but no code path writes it today** — see drift #1.
- **`unit_progress.state_code`** (free text, no CHECK): code writes `not_started | in_progress | complete | verified` (matches `gates.ts`'s `ProgressState`).
- **`gates.ts::GateState`** (real, used by `deriveGate`): `OPEN | CLOSING | CONDITIONAL | EXCEPTION_ONLY | HARD_CLOSED` — **matches spec 08's five states exactly, no drift**.
- **32-module permission matrix** (`seed/permissions.ts`): `dashboard, customer_overview, customer_journey, customer_tasks, customer_documents, customer_financials, customer_loan, customer_legal, customer_registration, customer_unit_readiness, customer_snags, customer_commitments, customer_communications, customer_handover, customer_activity, customer_audit, sales_handover, documents, collections, loans, legal, registrations, unit_readiness, snagging, handovers, commitments, communications, escalations, approvals, notifications, comments, reports, administration`. **Module is `unit_readiness`, never `unit_progress`** — see drift #3.
- **Event registry** (`events/registry.ts`): Appendix B names + sanctioned extensions, each flagged `built: true/false`. `built:false` rows (e.g. `document.requested`, `loan.sanction_received`, `commitment.created`, `escalation.raised`, `handover.scheduled`, `warranty.case_opened`) are real future hooks for R1+ specs — not drift, just not wired yet.

## Tables that already exist and a later spec proposes to "create" (name collisions)

These are the load-bearing findings: a spec's migration file (e.g. `0007_changeability.sql`)
**cannot literally `CREATE TABLE`** these — the table already exists from `0000_init.sql`
with a different, narrower column set. The spec's owning migration must `ALTER TABLE` and
reconcile naming instead.

| Table | Real columns (0000_init.sql) | Spec that re-proposes it | Spec's columns | Resolution |
|---|---|---|---|---|
| `component_definition` | `code, label, sort_order` | 07-unit-progress-control | `id, code, name, parent_code, product_types[], readiness_weight, sort_order, evidence_required, effective_from/to` | ALTER to add the new columns; spec's `name` **is** the real `label` — fixed in spec text (drift #2). |
| `change_category` | `code, customer_label, customer_visible, sort_order` | 08-changeability-engine | `code, name, product_types[], trade, default_lead_days, customer_visible, sort_order` | ALTER to add `product_types[]/trade/default_lead_days`; spec's `name` **is** the real `customer_label` — fixed in spec text (drift #4). |
| `change_gate_rule` | `id, category_code, trigger_component_code, min_state, resulting_state` | 08-changeability-engine | `id, category_code, project_id, trigger_component_code, trigger_state, trigger_event, condition_expr, resulting_state, hard_or_soft, closing_lead_days, exception_authority_role, priority, effective_from/to, version` | ALTER to add the new columns; spec's `trigger_state` is an enum-typed rename of the real `min_state` (a `ProgressState` string) — reconciled in spec text (drift #4), `gates.ts::deriveGate` keeps reading `min_state` until 08 lands and migrates it. |
| `unit_progress` | `unit_id, component_code, state_code, updated_at` (PK unit_id+component_code) | 07-unit-progress-control (as `unit_progress_state`, a *different* table name) | `unit_id, component_code, state_code, pct, actual_date, planned_next_event(_date), source, updated_by, updated_at, freshness_status, stale_after_days` | Not a collision (different name) but the same *entity* — 07 must ALTER `unit_progress` in place (rename is optional) rather than create a parallel table, or the two will drift apart. Flagged, not yet fixed in spec text — needs Amarsh's call on rename vs ALTER before 07 is built. |

## Known drift (found 2026-09-05, R0.5)

1. **Spec 06 rule 1** said a journey instantiates "On `booking.status → CRM_ACCEPTED`". The real `acceptBooking` (`bookings-crm.ts`) transitions `submitted → active` directly and never writes `crm_accepted` — confirmed by reading the function body. `crm_accepted` is a legal DB value (`booking_status_check`) and a graph edge in `model/status.ts`, but nothing produces it. **Fixed**: spec 06 rule 1 now triggers on the real, already-emitted event `sales_handover.accepted` instead of a status value nothing writes; a note tells 17's rebuild to either retire the unused `CRM_ACCEPTED` status value or start actually using it, and to keep 06 in sync either way.
2. **Spec 07** assumed `component_definition.name`; the real column is `label`. **Fixed** in spec text (kept `label`, dropped the invented `name`).
3. **Spec 07** assumed a permission module named `unit_progress`; the real module (in the 32-module matrix, already live behind R0.6's `authorize()`) is `unit_readiness`. **Fixed** in spec text.
4. **Spec 08** assumed `change_gate_rule` columns `trigger_state`/`trigger_event` where the real table has one column, `min_state` (a `ProgressState`, not a `GateState`); assumed `change_category.name` where the real column is `customer_label`; assumed `product_types[]`/`trade`/`default_lead_days` don't exist on either table. **Fixed** in spec text: renamed `name`→`customer_label` in the Data table, noted `min_state` as the real trigger column with `trigger_event` kept only for the non-progress triggers (procurement/drawing/handover events) that have no existing analogue, and flagged the new columns as additive.
5. Two stale code comments contradicted merged reality, and rechecking them surfaced a real gap:
   - `0001_identity.sql`'s `auth_event` comment said to fold into `event` "once 02 lands" — 02 has landed. **Fixed**: comment now gives the still-true reason to keep it separate (auth events aren't project/entity-scoped; `event`'s typed registry + append-only trigger don't fit free-form auth event types), not a stale "once X lands" promise.
   - `0002_event.sql`'s `event.actor_user_id` comment said it's nullable "because no user table exists yet" — 01 has landed, so I checked whether `appendEvent()` call sites now populate it. **They don't, anywhere** — grepped every domain handler (`bookings-crm.ts`, `tower-view.ts`, `warranty.ts`, `qa.ts`, etc.); none pass `actor_user_id`/`actor_kind` to `appendEvent`, so every real event today is stamped `actor_kind='SYSTEM'`, `actor_user_id=null` regardless of who actually acted — even though R0.6 put `ctx.actor` inside every one of those handlers. This is a real, verified gap, not just a stale comment. **Comment fixed** to state the gap plainly; **not fixed in code** (out of R0.5's schema-docs scope) — logged as new TODO row R0.6c: thread `ctx.actor` into every `appendEvent()` call's `actor_user_id`/`actor_kind`.

## Not drift (checked, spec text is fine as-is)

- `gates.ts::GateState` vs spec 08/24's five-state assumption — **exact match**, no fix needed.
- Every table a spec 09–31 proposes that has no existing same-named table (the large majority) — these are genuinely new, unbuilt features; nothing to reconcile until their migration is written, at which point re-check this file for anything the *next* migration adds that a later spec also assumes.
