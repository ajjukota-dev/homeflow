# 07 — Unit Progress Control (Unit Digital Twin: physical state)

## Purpose
p5 §4.1 Unit Digital Twin; p13 §8.7.1 "Unit Progress Control"; p33 §30.1 `UnitProgressState`; p43 §33.2 Project Unit Status Console: Project → Phase/Tower → Floor → Unit drill-down, **bulk update with affected-unit/gate preview and unit-level exceptions**, source + timestamp on every state; freshness threshold → "Verification Required" (p34 §30.3); "No API or UI path available to Sales/CRM may directly mutate UnitProgressState" (p44 §33.6 t3).

## Data
| Table | Columns |
|---|---|
| `component_definition` (real table since `0000_init.sql`: `code, label, sort_order` — this ALTERs it, it does not create it; see `SCHEMA.md`) | `code` (CIVIL_STRUCTURE, BLOCKWORK, PLASTER, ELECTRICAL_CONDUIT, ELECTRICAL_WIRING, PLUMBING_ROUGH_IN, PLUMBING_FIXTURES, FLOORING, KITCHEN, WARDROBES, DOORS, WINDOWS, PAINTING, SANITARY, HVAC, EXTERNAL_WORKS, COMMON_AREA_DEPENDENCIES, UTILITIES, CLEANING …), `label` (the real column — do not add a duplicate `name`), `parent_code` (room/trade/system hierarchy, p16 §8.8, new), `product_types[]` (PLOT: only boundary/road/utilities components, new), `readiness_weight` **[E §10.2 seed weights]** (new), `sort_order` (real), `evidence_required bool` (new), `effective_from/to` (new) |
| `unit_progress_state` — **decision needed before this is built**: the real table today is `unit_progress` (`unit_id, component_code, state_code, updated_at`, PK on unit_id+component_code). Either ALTER `unit_progress` in place to add the columns below (preferred — same entity, avoids a second source of truth) or get an explicit call from Amarsh to run both tables in parallel. Do not silently create a second table with a different name. | `unit_id`, `component_code`, `state_code ∈ {NOT_STARTED, IN_PROGRESS, COMPLETE, VERIFIED, REWORK}`, `pct int` (0–100, from checklist ticks or explicit for structure), `actual_date`, `planned_next_event`, `planned_next_event_date` (from 06 forecast or manual), `source ∈ {SITE_ENTRY, QA_VERIFICATION, BULK_UPDATE, IMPORT, SYSTEM}`, `updated_by`, `updated_at`, `freshness_status ∈ {FRESH, STALE, VERIFICATION_REQUIRED}` (derived), `stale_after_days` (from config) — p33 §30.1 |
| `progress_bulk_update` | `id`, `project_id`, `scope jsonb` (node ids / unit ids), `component_code`, `new_state`, `preview jsonb` (affected units, gates that would change), `exceptions jsonb` (unit ids excluded + reason), `applied_at`, `applied_by`, `status ∈ {PREVIEWED, APPLIED, CANCELLED}` |
| `progress_reopen` | `unit_id`, `component_code`, `from_state`, `to_state`, `reason`, `actor`, `at` (state regression always reasoned — TODO "V3") |

## Rules
1. Only roles with WRITE on module `unit_readiness` (the real 32-module permission-matrix name already live behind R0.6's `authorize()` — this spec previously said `unit_progress`, which is not a real module) (`SITE`, `QA`, `MANAGEMENT`, `SUPER_ADMIN`) may write; Sales/CRM/Customisation get `forbidden` — at the authorize layer **and** a handler-level guard (defence in depth, p44 §33.6 t3, p35 §30.5 t10).
2. Every write records `source`, `updated_by`, `updated_at`; the console shows them on every cell (p43 §33.2 "source + timestamp").
3. Regression (COMPLETE → IN_PROGRESS, VERIFIED → REWORK) requires `reason`; writes `progress_reopen`; emits `progress.reopened`.
4. `VERIFIED` may only be set by `QA` (site-declared vs QA-verified are distinct states, p16 §8.8); `COMPLETE` by Site.
5. Bulk update is two-step: `preview` returns affected units, the resulting gate states per unit (calls 08's evaluator in dry-run), and conflicts (units with a newer per-unit entry); `apply` excludes `exceptions` and stamps `source = BULK_UPDATE` (p43 §33.2, p44 §33.6 t2 "Bulk update shows affected gates before commit").
6. Freshness: `STALE` when `updated_at` older than `stale_after_days` for that component while `IN_PROGRESS`; `VERIFICATION_REQUIRED` when a gate depends on it and it is STALE (p34 §30.3 "Freshness threshold → Verification Required"). Derived at read; a nightly job emits `progress.stale` actions for the site owner (10).
7. Every change emits `progress.updated` with before/after; subscribers: 08 (gate re-evaluation), 14 (readiness recompute), 06 (forecast).
8. Progress percentages are never typed for interior components — derived from checklist/evidence in 15; explicit `pct` is allowed only for `CIVIL_STRUCTURE`-family components (structure by slab count) **[ours, honours p32 §27 "no manual progress %"]**.

## API
`GET /projects/:id/progress?node_id` (matrix units × components with state, source, timestamp, freshness) · `PUT /units/:id/progress/:component {state_code, pct?, actual_date?, planned_next_event?, reason?}` · `POST /projects/:id/progress/bulk/preview {scope, component_code, new_state}` → preview id · `POST /progress/bulk/:id/apply {exceptions[]}` · `GET /units/:id/progress` · `GET /units/:id/progress/history`.

## Screens
**Project Unit Status Console** (workspace → Site): tree Project → Phase/Tower → Floor; grid units × components; cell shows state chip + freshness dot + tooltip (source, who, when); filters by component/state/freshness; bulk-update drawer (select scope, component, state → preview table with gate deltas → exclude units with reason → apply); reopen dialog with reason. Unit 360 → Progress tab with history. Mobile (375): unit list → per-unit checklist view. Read-only rendering for Sales/CRM (no controls rendered, not just disabled).

## Events
`progress.updated`, `progress.reopened`, `progress.bulk_applied`, `progress.stale`.

## Config
`component_definition` (per product type), `stale_after_days` per component, weights — Policy Studio "Unit progress components", "Freshness thresholds".

## Acceptance
p35 §30.5 t10 · p44 §33.6 t1 ("Site updates a component → dependent gates re-evaluate"), t2, t3, t5 ("Stale progress flags Verification Required"), t6 ("Reopen requires reason and is audited") · rule tests 1–8 · Playwright console at 1440/768/375.

## Depends on / Feeds
Depends on 01, 02, 04. Feeds 08, 14, 15, 24, 28.

## Files
`services/api/src/progress/**`, `services/api/migrations/0006_progress.sql`, `services/api/src/seed/components.ts`, `apps/workspace/src/pages/site/UnitStatusConsole*.tsx`, `apps/workspace/src/pages/site/BulkUpdate*.tsx`.

## Not in this feature
Gate rules/states (08), QA evidence capture (15), readiness scores (14).

## Build note (2026-09-06, backend)
The Data table's open decision is taken as the row itself prefers: `unit_progress` is ALTERed in place (`0031_progress.sql`; 0006 was taken) — no `unit_progress_state` table. `state_code` keeps its real lowercase vocabulary plus `rework`; the spec's UPPERCASE names translate at the API boundary (`progress/core.ts`), same as booking/unit status. Component codes stay the four real seeded ones (`structure`, `mep_first_fix`, `flooring`, `finishing`) that `change_gate_rule` FKs onto — the uppercase list above is a Policy Studio re-seed proposal; "CIVIL_STRUCTURE family" (rule 8) is `structure` + children via `parent_code`. Rule 1 built to the seeded matrix: MANAGEMENT holds READ on `unit_readiness`, so it cannot write progress; FM holds WRITE. Rule 3's "regression" = a downgrade from COMPLETE/VERIFIED (the two literal examples). Rule 5's dry-run uses the real gate engine (`gates.ts::deriveGate`), not a stub. Rule 6's nightly job is `scanStaleProgress(asOf)`, callable and tested — no scheduler exists. `handlers.ts::setProgress` is now a compatible wrapper over `updateProgress`. Screens and Studio tabs deferred; `seed/components.ts` not created (column defaults carry the config; no real per-component weights/thresholds to seed).

## Build note (2026-09-06) — Policy Studio tabs
`07.progress_components` and `07.freshness_thresholds` both registered against `component_definition` (one table, two tab keys) in the generic draft/publish/history envelope — zero new frontend code, same shape as `10.action_types`. Full detail (the cross-spec batch this shipped in, plus a real `GenericTableEditor` bug found and fixed while verifying it live) is in `TODO.md` §9 and `docs/demo/run-log.md`'s 2026-09-06 "Studio registry-only batch" entry.

## Build note (2026-09-07, UI)
Built the **Project Unit Status Console** as a new "Console" tab on the existing `SiteProgress.tsx` page (workspace → Project / Site), sitting alongside the page's pre-existing "By villa" single-unit view (kept, untouched in structure — `visual.spec.ts`'s "Site + Sales" test asserts on its `h1`/tab). New files under `apps/workspace/src/pages/site/`: `api.ts` (typed client), `labels.ts` (state/freshness chip meta, `WRITE_ROLES`, `canSetState`), `ProgressConsole.tsx` (grid + filters + mobile unit-list/checklist), `CellEditDialog.tsx` (single-cell edit with regression-reason enforcement), `BulkUpdateDrawer.tsx` (scope → component → state → preview-with-gate-deltas → exclude-with-reason → apply).

**Real role matrix over spec prose (rule 1):** the spec's own Data table already corrected rule 1's role list once (MANAGEMENT is READ, FM is WRITE) but `apps/workspace`'s new `WRITE_ROLES`/`canSetState` constants were first written against the spec's stale prose (`SITE, QA, SUPER_ADMIN`, no FM). Caught and fixed against the real seeded matrix (`seed/permissions.ts`'s `unit_readiness` row: `SITE=W, FM=W`, QA mirrors the SITE column, `SUPER_ADMIN` gets WRITE everywhere) before landing — `canSetState`'s COMPLETE/VERIFIED branches now exactly mirror `core.ts::assertStateAuthority`, and the "everything else" branch includes FM. FM has no nav entry point to this page today (out of this slice's scope — flagged, not fixed), so this was a dormant correctness gap, not a live one.

**Read-only rendering, page-wide, not just the new tab:** the directive was "read-only rendering for Sales/CRM/Customisation... literally don't render write controls" for the Console. Live-testing as `management@demo.pranava` (MANAGEMENT: real READ on `unit_readiness`, and — unlike SALES/CRM/CUSTOMISATION — does have nav access to this page) surfaced that the **pre-existing** "By villa" tab had no role gating at all: its `Segmented` progress control rendered fully clickable radio buttons for any role that could reach the page, which would 403 silently on click (`setState` has no `.catch`). Backend enforcement was always correct (`assertProgressWriter` → `authorize(ctx, "unit_readiness", "WRITE")`); this was a UI-only gap. Fixed by gating "By villa"'s controls on the same `canWrite` check as the Console (`SiteProgress.tsx`), rendering a plain read-only chip instead of the `Segmented` control for non-write roles — kept a small, additive diff rather than adding a `disabled` prop to the shared `packages/ui` `Segmented` component.

**Bug found and fixed via live Playwright verification, not just code review:** `BulkUpdateDrawer.tsx`'s `apply()` originally gated on the stale `preview.requires_reason` (computed once, before any per-unit exclusions). Excluding every regressing unit with its own reason left `apply()` permanently blocked with "regresses some units" even though the backend (`core.ts::applyBulkUpdate`, lines ~335-336) correctly recomputes the regression check against only the units actually being applied. Fixed the client to mirror the backend exactly (`preview.units` filtered by `!no_op && !excluded`, checked for `.regression`) — verified live end-to-end: raise two units, exclude two already-verified units with a reason, apply succeeds and only the non-excluded units change.

**Raw-id leak (this session's recurring bug class) found and fixed proactively:** `progress/core.ts::getUnitProgress` exposed `updated_by` as a bare user id. Added a `LEFT JOIN "user"` for `updated_by_name`, threaded through `ProgressCell` end-to-end (API type, tooltip, mobile checklist, cell-edit dialog's "Last: ... · Amarsh (Super Admin)" line). Verified live: a freshly-written cell shows the real actor name; a pre-existing seeded cell with a genuinely `NULL updated_by` correctly shows no name (not a fallback bug).

**Scope cuts:** no Unit 360 tab — that page doesn't exist yet (matches specs 18/16's own precedent this session); the console's cell tooltip/dialog carries the same source+who+when info Unit 360 would have shown. Mobile (375px) is a real unit-list → per-unit-checklist view, not a squeezed grid, per the spec's own screens line.

**Verification:** live-verified via Playwright MCP against the real backend — grid rendering, all three filters, single-cell edit (forward + regression-with-required-reason, confirmed a real `progress_reopen` row + `progress.reopened` event), bulk update (scope → preview with real gate deltas `structural: OPEN→HARD_CLOSED` → exclude-with-reason → apply), read-only rendering for MANAGEMENT on both tabs, and all three breakpoints (1440/768/375) with no console errors and no horizontal overflow. New `apps/workspace/e2e/progress-console.spec.ts` (6 tests, all passing): 3-breakpoint smoke, single-cell regression-reason enforcement (asserted via `GET .../progress/history` for a real `progress.reopened` event), bulk-update preview/exclude/apply (against an isolated fixture hierarchy node + two fixture units, not shared seed state, to stay deterministic), and MANAGEMENT read-only rendering (`table button` count 0, no `radiogroup`s, no "Bulk update" button). `npx tsc --noEmit` clean in both `apps/workspace` and `services/api`. Full regression from a fresh `db:reset`: backend 789/789 (2 files — `documents.test.ts`, `registration.test.ts` — time out under full-parallel load, same known pre-existing artifact documented in spec 27's build note; both pass 16/16 in isolation); full Playwright e2e suite (146 tests, including the 6 new `progress-console.spec.ts` tests) — 145 passed, 1 pre-existing skip, 0 failed.

**Freshness dot was colour-only — fixed (found via advisor review, not by any test):** the cell's freshness marker (`Cell` in `ProgressConsole.tsx`) rendered STALE and VERIFICATION_REQUIRED as a bare coloured dot with no visible label except on tooltip hover — distinguishable only by hue, against CLAUDE.md's "status never by colour alone (icon + label)" bar. `design.spec.ts`'s axe run only covers `packages/ui` preview pages, never app pages, so nothing in the suite would have caught this. Fixed: `FRESHNESS_META` (`labels.ts`) now carries an `Icon` (`Clock` for STALE, `AlertTriangle` for VERIFICATION_REQUIRED, `null` for FRESH — the unmarked default) alongside its label/className, and the cell renders that icon instead of a plain dot. `npx tsc --noEmit` clean; re-verified all three breakpoints.

**Second-pass e2e run without a `db:reset`, to check the new spec's fixtures don't degrade a re-run (advisor-suggested check):** ran the full suite again against the already-populated DB from the prior run (no reset). Result: 136 passed, 10 failed — but all 10 failures are in pre-existing, unrelated specs (`customer-updates.spec.ts` empty-state assertions, `commitments.spec.ts`, `admin-model.spec.ts` merge-preview counts, `handover-gates.spec.ts`, `visual.spec.ts`'s booking/QA-handover flows), none touching unit progress/readiness data — the same known "suite must run from a fresh DB, a second run looks broken only from accumulated fixtures" characteristic already documented for spec 16 (`TODO.md`). All 6 `progress-console.spec.ts` tests passed cleanly in this second, unreset run, confirming this spec's own fixtures (uniquely-named per run) don't accumulate into failures for itself or leak into other specs. No fix needed; this was a verification-only check. Final re-run after the freshness-icon fix, from a fresh `db:reset`: backend 789/789 (same 6-test `registration.test.ts` timeout-under-parallel-load artifact, 6/6 in isolation); Playwright 145 passed, 1 pre-existing skip, 0 failed (146 total) — unchanged from the earlier run, confirming the freshness-icon change caused no regression.

**Codex review flagged `BulkUpdateDrawer.tsx`'s Back button — verified live as a false positive:** "Selecting Back after a preview leaves the old scope/reason intact — User can preview one node, click Back, change only visible fields, and unknowingly apply/preview with stale node selections or regression reason from the previous preview." Static read: `Back` (`setPreview(null)`) intentionally keeps `nodeIds`/`componentCode`/`newState`/`reason` in place (normal "keep my inputs" UX), but `apply()` only ever acts on the `preview` state object, and the Apply button's entire JSX branch (lines 118-161) only renders when `preview` is non-null — so there is no code path back to Apply without first calling `runPreview()` again, which regenerates `preview` from whatever the field values are *at that moment* and resets `excluded` to `{}`. Verified live via Playwright MCP against the running app (not just re-reading the code): opened Bulk update as SUPER_ADMIN, scoped to "Ungrouped units", previewed (2 units, no regression), filled an exclude-reason on unit "1A", clicked Back (form fields preserved, no Apply button anywhere in the DOM), added "Phase 1 — Villas" to the scope, clicked Preview again — the new preview correctly showed the expanded scope (8 will change, 5 regress, matching the union of both nodes) and `Apply to 8 units` (not the stale "2"), and every one of the 8 exclude-reason inputs, including "1A"'s, read back as `""` via `document.querySelectorAll` — the earlier reason did not leak. Confirms the false positive: Apply always operates on data consistent with the currently-displayed preview, and `excluded` never survives a Back → re-preview cycle. No code change made for this finding. Closed the drawer without applying, leaving the dev DB untouched.
